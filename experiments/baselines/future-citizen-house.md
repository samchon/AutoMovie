# future-citizen-house

`benchmark-four` 사이클, 2026-08-18. 관측자(드라이버)가 저작 에이전트에게
브리프만 주고 라운드마다 디스크를 직접 재서 판정한 실험의 기준선.

**이 사이클의 헤드라인: 지난 캠페인을 죽인 스타터 교체 벽을 턴 1 안에 넘었고,
S1(매싱)이 네 샌드박스 중 처음으로 닫혔다.**

---

## 1. 정체

**무엇을 짓게 했나.** 실재하는 2층 단독주택 한 채. 약 11 × 11.6 m, 층고 3.2 m,
연면적 약 250 ㎡. 참고 이미지 5장(외관·섹션 아이소메트릭·거실·플렉스룸·상층부)을
브리프의 내용물로 지정했다.

**고정 그래프(브리프가 못 박은 것).**

| 층 | 방 |
| --- | --- |
| ground | entry, stair-bay, flex-workroom, common-room(거실·식당·주방 연속), ground-services-core |
| upper | upper-corridor, primary-bedroom, bedroom-two, bedroom-three, family-bathroom, upper-storage |

**단계.** S1 매싱 → S2 셸(커튼월·PV 캐노피·불투명 외장) → S3 실내 → S4 마감.

**관측 규율.** 드라이버는 저작하지 않는다. 라운드마다
`design` → `compile` → `review:status` → `building:report` → 해시·바이트 비교 →
체크포인트 순으로 재고, **결함 하나만** 관측 형태로 보낸다 — 얻은 방법을 밝히고,
원인을 말하지 않고, 도구 이름을 쓰지 않는다.

**저작 에이전트.** `codex exec` (`gpt-5.6-sol`, reasoning effort `xhigh`),
세션 UUID `01a01256-10a3-7472-92e4-7b85122289ee`, 샌드박스 `workspace-write`.

---

## 2. 규모

라운드 1 기준값과 **라운드 4**(턴 4 경계, revision 80) 값을 함께 적는다.
움직이지 않은 행이 S1이 닫힌 뒤 무엇이 고정됐는지를 말해준다.

| 항목 | 라운드 1 | **라운드 4** |
| --- | --- | --- |
| 소스 파일 (`src/**/*.ts`) | 29 | **31** |
| 문서 (`docs/**`) | 8 | **15** |
| 배송 프레임 (`renders/**/*.png`) | 43 (15개 shot) | **49** (15개 shot) |
| 해시 대상 아티팩트 전체 | 216 | **234** |
| design 레코드 | 16, unowned 0 | **19**, unowned **0** |
| 컴파일된 공간 | 14 (room 11 + storey 2 + building 1) | **14 (불변)** |
| 개구부 / 커넥터 | 14 / 10 | **14 / 10 (불변)** |
| 건축 도면 | 6장 + `report.json` | **6장 + `report.json` (불변)** |
| 제품이 스스로 선언한 미지원 격차 | 36건 | **36건 (불변)** |
| revision | 68 | **80** |
| `review:status` 리뷰 대상 | 20건 전부 `missing` | **48건 전부 `missing`** |
| 저작자 신고 요소 | 81 (미검증) | **140 (미검증)** |

**S1 총계(공간·개구부·커넥터·도면·격차)가 네 라운드 내내 한 번도 움직이지 않았다.**
S2가 커튼월과 캐노피를 얹는 동안 닫힌 단계의 수치가 고정돼 있었다는 뜻이고,
이것이 단계 분리가 실제로 지켜졌다는 가장 싼 증거다.

**리뷰 대상 20 → 48의 내역** (`review:status`에서 직접 셈):
design 18 · shot 15 · prototype 12 · source 1 · sequence 1 · film 1.

> **이 증가를 제품 변경으로 설명하려던 것을 철회한다 — 이 실행의 여섯 번째 자기 정정.**
> `#1980`("모든 프로토타입은 자기 리뷰를 빚진다")이 원인이라고 먼저 적었다.
> **git이 반증했다: `#1980`은 팩 기준 커밋보다 4커밋 앞선다.** 라운드 제로의 20에도
> 이미 적용돼 있었다. 20은 **스타터 프로덕션**(기하 0, shot 구성이 다름)에서 잰 값이고
> 48은 15-shot 프로덕션의 값이므로, 둘의 차이는 **설계 성장**이다.
> 애초에 비교 가능한 두 수가 아니었다.

**프로토타입 리뷰 12건은 전부 한 shot(`s1-bedroom-three`)에 걸린다** — 프로토타입 ×
shot의 곱이 아니라 프로토타입당 1건이다. `s2-canopy-frame`·`s2-solar-module` 같은
**외부** 요소도 그 침실 shot에 걸리는데, 결함이 아니다: 주체 검사 뷰어의 눈은 선언된
셀이 아니라 **주체 자신의 content extent**에서 유도되므로 shot은 staging 맥락일 뿐이다.

**15개 shot:** front, three-quarter, side, rear + named space 11개 각각 하나.
**섹션 컷은 shot이 아니라 검사 뷰**로 `reports/future-house/section-cut.png`에 있다.

---

## 3. 비용

### 턴 (저작 에이전트)

| 턴 | 소요 | 결과 |
| --- | --- | --- |
| 1 | **약 88분** | S1 전체 저작 + 43 프레임, 호스트 프로세스 고갈로 중단 |
| 2 | **약 20분** | 섹션 컷 1장 |
| 3 | **약 31분** | S2 커튼월 부재 시스템 |
| 4 | **17.3분** (rollout의 `duration_ms` 1,040,337) | S2 PV 캐노피 |
| 5 | 진행 중 | S2 불투명 외장 (셸 마지막 항목) |

**턴 소요는 rollout의 `task_complete` 레코드가 `duration_ms`로 직접 낸다.**
벽시계를 따로 재거나 래퍼 로그의 타임스탬프를 빼지 않는다 — 그 레코드가 권위다.

### 명령 (드라이버 측, `npm.cmd run` 래퍼 포함 — 아래 주의 참조)

| 명령 | 라운드 1 | 라운드 2 | **라운드 4** |
| --- | --- | --- | --- |
| `design` | 29초 (exit 0) | 16초 (exit 0) | **3초** (exit 0) |
| `compile` | 169초 (exit 1) → 124초 (exit 0) | 34초 (exit 0) | **17초** (exit 0) |
| `review:status` | 161초 (exit 1) | 49초 (exit 1) | **20초** (exit 1) |
| `building:report` | 45초 (exit 0) | 23초 (exit 0) | **16초** (exit 0) |

**라운드 1 대비 라운드 4가 5~10배 빠르다.** 프로덕션은 그 사이 계속 커졌으므로
(요소 81 → 140) 이것은 설계가 작아져서가 아니라 **머신 부하가 걷혀서**다.
라운드 1은 자유 메모리 1.4 GB · `node.exe` 40개에서 쟀다.
**부하를 함께 적지 않은 벽시계는 비교가 불가능하다는 규칙의 실측 사례다.**

> **주의: 이 수치는 저작 에이전트가 겪는 비용이 아니다.**
> 저작 에이전트는 `npm run`을 전혀 쓰지 않고 `scripts/*.ts`를 node·tsx·ttsx로
> 직접 몬다. 위 값에는 npm 래퍼 오버헤드가 얹혀 있다.

> **주의 2: 라운드 1 수치는 메모리 고갈 상태(자유 1.4 GB, `node.exe` 40개)에서
> 쟀다. 상한이지 깨끗한 값이 아니다.** 라운드 2가 정상 부하에 가깝다.

---

## 4. 도달

### 넘은 벽 — 스타터 교체가 턴 1 안에 끝났다

지난 캠페인은 **두 턴 90분에 기하 0**이었고 전부 스타터 걷어내기에 들어갔다.
이번 턴 1은 `design` **exit 0, unowned 레코드 0건**으로 끝났다.
`soloist`·`chorus*`·`opening`·`answer` 잔재 **0건**. 세 층이 전부 교체됐다.

### S1이 닫혔다 — 근거

| 종료 조건 | 근거 |
| --- | --- |
| 모든 방이 존재 | 스케줄 14 space, 고정 그래프와 일치 |
| 모든 방이 층에 속함 | room 11개 전부 `parent: ground-floor \| upper-floor` |
| 끊김 없이 도달 가능 | **섹션 컷**이 두 슬래브와 그것을 잇는 계단·참을 보여줌 |
| 치수가 강제되는 불변식 | `assertHouseDimensions()`가 `design()`의 **첫 문장**, 면적 드리프트·라이저 180 mm 초과에서 throw |

envelope 11.0 × 11.6 m, 층고 3.2 m, 연면적 **255.2 ㎡**.

### 증분 전파 — 잡음이 없다 (결정론 주장은 아래처럼 좁혀서 읽어야 한다)

> **주의: 프레임의 "CHANGED 0"을 결정론의 증거로 쓰지 마라.**
> 프레임은 **내용 주소**로 저장된다. `shot-s1-front` 하나에 다이제스트 디렉터리가
> 4개 있다. **재렌더는 새 경로를 만들고 옛 프레임을 덮지 않는다.**
> CHANGED 0은 주소 체계가 보장하는 구조적 결과다.

**실제 증거는 `generated/`의 거동이다.**

| 라운드 | 설계 변경 | 전체 CHANGED | `generated/` | 프레임 ADDED |
| --- | --- | --- | --- | --- |
| r1 → r2 | **없음** | **0** / 216 | **0** | 0 |
| r2 → r3 | **있음** (커튼월) | 22 / 222 | **19** | 2 (front, three-quarter) |
| r3 → r4 | **있음** (PV 캐노피) | 20 / 234 | **18** | 4 (front, side ×2, three-quarter) |

**설계가 안 움직이면 파생물이 하나도 안 움직이고, 설계가 움직이면 그것에 의존하는
파생물만 움직인다.** `building:report` 수치는 네 라운드 모두 동일했다.

> **r3의 "front·three-quarter만 건드렸다"를 정정한다.** r4에서 캐노피 하나가
> **15개 shot의 컴파일 레코드를 전부** 움직였다. 이유는 회귀가 아니라 구조다 —
> `deriveBuilding.ts`의 `staged()`가 보여주듯 **컴파일러는 built environment 레코드를
> 그것을 staging하는 모든 shot에 복사한다.** 그러므로 집 기하가 바뀌면 실내 shot의
> 레코드도 바뀌는 것이 정상이다.
> **움직인 프레임은 저작자가 실제로 다시 렌더한 3개뿐이다.**
> `generated/`의 팬아웃과 렌더된 프레임 수를 같은 것으로 세면 안 된다.

### 프레임이 최신인지 재는 법 — 제품이 답을 이미 갖고 있다

드라이버가 "어느 프레임이 지금 설계의 것인가"를 서술이 아니라 **계산**으로 답할 수 있다.

| 어디에 | 무엇이 |
| --- | --- |
| `generated/<production>/manifests/compile.json` | `inputFingerprint` — **현재** 컴파일 지문 |
| `renders/**/<digest>/<digest>/manifest.json` | `compileFingerprint` — **그 프레임이 렌더된** 컴파일 지문 |

둘을 맞춰보면 된다. 턴 4 경계 실측: **15개 shot 중 3개만 현재 컴파일의 프레임을
갖고, 12개는 이전 컴파일의 것이다.** 저작자가 외부 3개만 다시 렌더했으므로 정확하다.

> **이 계측기가 나를 한 번 속였다.** `compile.json`에 `fingerprint` 필드가 있다고
> 가정하고 읽었더니 `undefined`가 나왔고, 그것이 어떤 프레임과도 매치되지 않아
> **"15개 shot 전부 stale"이라는 그럴듯한 결과**를 냈다. 실제 필드명은
> `inputFingerprint`다. **없는 키를 읽은 비교는 실패하지 않고 전부 불일치로 답한다.**
> 키 이름을 먼저 출력해 확인하지 않았으면 이 실행은 존재하지 않는 결함을 보고했다.

### 저작 에이전트의 자기 보고는 정확했다

섹션 컷에 대해 신고한 SHA-256 `07932e5f…89869`와 1280×720이
**실제 파일과 바이트 단위로 일치**했다. 스스로 버린 것도 밝혔다
(자동 섹션 평면이 파사드 밖으로 떨어져 폐기, three-quarter 비교 2건 폐기).
`reports`에 둔 이유까지 댔다 — 섹션은 검사 뷰이지 저작 배송 shot이 아니다.

### 참고 이미지 대조 (S2 이후의 과제)

| 항목 | 참고 | 현재 |
| --- | --- | --- |
| PV 캐노피 | 경사 오버세일, 개별 모듈, 파사드 밖 큰 돌출 | 얇은 평지붕판 |
| 커튼월 | 멀리언·트랜섬 격자, 투명/반투명 교대 | **zone 단위는 저작됨**, 부재 세분 미저작 |
| 외장 톤 | 밝은 라임스톤 | 한 면이 뚜렷하게 어두운 갈색 |

---

## 5. 판정

**S1은 닫혔다. 제품은 이 규모의 건축 매싱을 저작 에이전트 한 명에게 맡길 수 있다.**
컴파일은 초록이고, 결정론은 바이트로 증명됐고, 제품이 자기 한계를 36건 스스로
선언한다("없는 마감 행을 미완성 방으로 읽지 마라", "스케줄된 계단을 누가 오를 수
있는 계단으로 읽지 마라"). 그 정직함이 이 제품의 최고 품질이다.

### 다음 실행이 **다시 시험하지 말아야 할 것**

1. **스타터 교체가 가능한가** — 가능하다. 턴 1 안에 끝난다. 다시 재지 마라.
2. **증분 전파에 잡음이 없다** — 설계 미변경 라운드는 `generated/` 0건,
   변경 라운드는 의존하는 19건만. 다시 재지 마라.
   **단 프레임 해시 비교로 결정론을 재려 하지 마라 — 내용 주소라 항상 0이 나온다.**
3. **치수 불변식이 강제되는가** — `design()` 첫 문장에서 throw한다. 확인됨.
4. **`approvals_reviewer` 없이 per-call MCP가 되는가** — 안 된다. 확정됐다(아래).
5. **schannel 클라이언트로 네트워크가 되는가** — 안 된다. node 번들 CA는 된다.
   참고 자산 URL은 샌드박스 안에서 도달 가능하다.
6. **`building:report`가 같은 입력에 같은 바이트를 내는가** — 낸다. 아래 대조군.
7. **아무것도 안 쓴 `design`이 revision을 올리는가** — 올리지 않는다. 아래 대조군.

### 결정론 대조군 — 라운드 간 비교가 아니라 **한 상태 안에서** 두 번 재라

라운드 간 비교는 그 사이에 저작 턴이 끼므로 결정론을 재지 못한다. 두 명령을
**아무것도 바뀌지 않은 같은 상태에서 연속으로** 돌리면 교란이 원리적으로 없다.

| 대조군 | 결과 |
| --- | --- |
| `building:report` 2회 연속, 사이에 변경 0 | **`reports/`의 8개 파일 전부 바이트 동일** |
| `design` (wrote 0 / unchanged 18) 전후의 `revision.json`·`compile.json` | **바이트 동일, revision 80 → 80** |

**형제 드라이버가 보고한 "`building:report`가 라운드마다 7/7 바뀐다"는 이 프로덕션에서
재현되지 않는다.** 여기서 바뀌는 유일한 조건은 설계가 실제로 바뀌는 것이다.
형제의 관측을 결함으로 올리기 전에 **같은 상태 안의 2회 실행**으로 먼저 갈라야 한다.

두 번째 대조군은 라운드 1의 `compile-input-changed`(revision 67 → 68) 미제와도 맞물린다.
**`design`이 revision을 올린다는 설명은 이제 직접 반증됐다.** 원인은 여전히 미상이고,
라운드 2·4가 재현하지 못했으므로 **`unverified`로 닫는 것이 최종 상태다.**

### 저작 턴이 살아 있는 동안 `compile`·`design`을 돌리지 않는다

드라이버의 라운드 명령은 프로젝트 커밋 락을 잡는다(`#1994`가 그 락의 표면이다).
**저작 턴과 경합하면 둘 중 하나가 거절당하고, 그 거절은 저작 실패로 오독된다.**
라운드는 **턴 경계에서만** 돈다. 읽기만 하는 계측(해시·프레임 열람·소스 grep)은
턴 중에 해도 안전하다.

### 확정된 환경 사실

- **per-call 등록 MCP 서버는 승인을 요구하고, 전역 등록 서버는 요구하지 않는다.**
  `approval_policy: "never"` + `approvals_reviewer: "user"`가 그 요구를 자동 거절한다.
  한 턴 안의 대조군으로 증명: 제품 표면(per-call) 0 ok / 3 err,
  `node_repl`(전역) 27 ok / 0 err, 같은 세션·같은 정책.
  **격리를 지키려면 `-c approvals_reviewer="auto_review"`가 필수다.**
- **스크래치패드는 드라이버 간 공유다.** generic 파일명(`turn2.log`, `turn2.sh`)이
  실제로 두 번 충돌했다. 신호는 UUID가 이름에 박힌 rollout 파일로 잡아야 한다.

### 프로세스 귀속 — **UUID 규칙 폐기는 턴 1에서만 쟀다.** 정정한다

캠페인이 `#1993`으로 "혈통이 규칙, UUID 문자열 매칭은 아무것도 못 찾는다(0 %)"를
`steering.md`에 박았다. **그 측정은 전부 턴 1의 것이고, 턴 1에는 UUID가 명령줄에
존재하지 않는다** — 첫 턴은 `codex exec`에 UUID를 인자로 주지 않기 때문이다.

**resume 턴에서는 정반대다.** 이 실행이 턴 5에서 실측:

| 방법 | 턴 1 | **턴 5 (resume)** |
| --- | --- | --- |
| 런처에서 내려오는 혈통 폐포 | 23개 (기준) | **1개 — 사슬이 끊긴다** |
| 세션 UUID 문자열 매칭 | **0개 (0 %)** | **3개, 전부 정확** (`sh.exe` → `node.exe` → `codex.exe`) |

턴 5의 실제 사슬: 내 런처 `sh.exe 19972` → **(표에서 끊김)** → `sh.exe 37068` →
`node.exe 26192` → **`codex.exe 42520`**. 중간 부모가 이미 종료해 `ParentProcessId`가
죽은 pid를 가리키므로, **런처에 뿌리내린 폐포는 자손 1개(자기 자신)를 돌려준다.**
그것은 "턴이 끝났다"와 구별되지 않는다 — 함정 3이 혈통 규칙 안에서 재현된 것이다.

**그 순간 내 프로세스를 찾아낸 유일한 방법이 캠페인이 폐기한 UUID 매칭이었다.**
같은 시각 `codex.exe 34104`가 UUID `01a01255-…`로 살아 있었다 — 형제 캠페인의
modern 드라이버다. **이름·모양·깊이로는 구별되지 않고 UUID로는 즉시 갈렸다.**

> **정정된 규칙: 턴 1은 혈통으로, resume 턴은 UUID 문자열로 귀속한다.
> 둘 중 하나가 빈 결과를 주면 그것은 "끝났다"가 아니라 "이 턴에서는 그 방법이 안 된다"이다.**
> 그리고 rollout 성장이 판정을 이긴다 — 폐포가 1일 때 rollout은 20초에 13,940 바이트
> 자라고 있었고, **살아 있다고 말한 것은 rollout뿐이었다.**

**이것이 캠페인의 여섯 번째 "그럴듯한 부분 진실"이고, 이번에는 제품이 아니라
캠페인 자신의 규칙에서 나왔다.** 두 드라이버가 한 조건(턴 1)에서 잰 것을
전 조건으로 일반화했다. 기준선의 다섯 자기 정정과 정확히 같은 모양이다.

### 관측 — `deriveBuilding.ts`의 근거 문장이 자기가 쓰는 7개 중 6개에 대해 거짓

**결함으로 올리지 않는다. 아래에 그 이유까지 적는다.**

`packages/template/scaffold/scripts/deriveBuilding.ts`의 JSDoc이 사이드카를 절대 지우지
않는 정책을 이렇게 정당화한다.

> Rename a building **or a view** and its old files stay where they were, still
> naming the revision they were derived from, **which is why the revision is in
> them** … Delete a stale folder yourself.

실측 (revision `sha256:fce8b55a…aad59f`):

| 파일 | revision을 담는가 |
| --- | --- |
| `report.json` | **담는다** — 최상위 `revision` 키 |
| `house-{plan,ceiling,section,elevation,services,finishes}.svg` | **6개 전부 담지 않는다** |

SVG가 갖는 것은 `data-automovie-digest` — **시트 자신의 내용 다이제스트**이지
그것이 유도된 설계 revision이 아니다. 즉 근거 문장은 **7개 중 1개에만 참**이다.
문장이 명시적으로 이름 댄 두 경우 중 **view 이름 변경**이 특히 어긋난다: 남는 것은
폴더가 아니라 **살아 있는 디렉터리 안의 고아 `.svg` 한 장**이고, "stale folder를 지워라"는
조언은 그 경우에 적용되지 않는다.

**그래도 결함으로 올리지 않는 이유 — 도달 가능성 작업을 끝까지 했기 때문이다.**
`report.json`이 `sheets[].view.id` 6개를 열거하므로, 고아 `.svg`는 **파일명을
그 목록과 대조하면 검출된다.** 건물 이름 변경 쪽도 옛 디렉터리가 자기 `report.json`을
갖고 그 안에 옛 revision이 있다. **따라서 독자가 막히지 않는다.**
`#1994`가 "행동할 수 없는 조언"인 것과 달리, 이것은 **부정확한 근거 문장**이다.
정확히 그만큼만 적는다.

> **이 구별이 이 실행의 판단이다.** 후보를 찾은 것과 결함을 찾은 것은 다르고,
> `#1997`이 남긴 교훈("필터링을 목록과 함께 배송하라")이 요구하는 것이 이 문단이다.
> 필터를 돌리기 전의 나는 이것을 `#1994`와 같은 종류로 적을 참이었다.

### 발행된 결함 — #1997 (관사 일치)

**이 실행이 캠페인에 돌려준 제품 결함.** `packages/engine/src/drawing/drawingSchedule.ts:210`이
보간값 앞에 관사를 하드코딩한다:

```ts
reason: `a ${subject} row states type, size and count but no place, …`
```

`subject`가 `"opening"`이면 출하되는 진단문이 **"a opening row"**가 된다.
내 `building.log` 라운드 1·2·3 모두에서 재현됐다.

**조율자가 필터링한 최종 범위:** 34개 후보 중 **도달 가능하고 실제로 틀린 것 10개**,
전부 `@automovie/engine`. 그 밖에 **숫자 앞 9개** — `a 8-byte chunk`가
"a eight-byte chunk"로 읽힌다. **나는 숫자 경우를 아예 생각하지 못했다.**

**내 grep이 만든 오탐 3건: `area ${axis}` 안의 부분문자열 `a ${`.** 관사 자리가 아니다.

> **교훈: 기계적 추출은 후보 목록이지 발견 목록이 아니다.**
> 나는 "34개는 후보이지 34개 결함이 아니다"라고 경계를 표시했고 그것은 옳았다.
> 그러나 **정규식 자체가 부정확했고, 도달 가능성 판정(가드·유니온·숫자)은
> 내가 하지 않고 조율자가 했다.** 후보를 던지는 것으로 일을 끝내지 마라.

### 이 실행의 진짜 방법론적 발견 — **자기 정정 다섯 건이 전부 같은 모양이었다**

| # | 내가 먼저 말한 것 | 측정이 말한 것 |
| --- | --- | --- |
| 1 | "낯선 `codex.exe` 15개" | 귀속 불가 14개 — 남의 것인지 알 수 없다 |
| 2 | "샌드박스에서 TLS가 불가능하다" | **schannel 클라이언트만** 실패, node 번들 CA는 된다 |
| 3 | "MCP 호출 3건이 성공했다" | 그건 `node_repl`이고 **제품 표면은 0/3** |
| 4 | "`design`이 revision을 올려서 compile이 거절했다" | design은 **두 라운드 다 0건** 썼다. 원인 미상 |
| 5 | "프레임 43장 0바이트 변화가 결정론의 증거다" | 프레임은 **내용 주소**라 구조적으로 항상 0이다 |

**모양이 하나다: 그럴듯한 인과를 측정보다 앞세웠다.**
다섯 번 다 증거는 부분적으로 참이었고 결론만 너무 넓었다.

**이것을 알아보는 것이 개별 정정보다 값지다.** 다음 실행은 결론을 쓰기 전에
이 질문을 통과시켜라:

1. **내가 잰 것이 정확히 무엇인가?** (파일 하나인가, 클라이언트 하나인가, 서버 하나인가)
2. **그것을 전체로 일반화하고 있는가?** 같은 계열 둘은 계열 전체의 증거가 아니다.
3. **0을 합계에서 읽고 있지 않은가?** 먼저 묶어라.
4. **이 값이 구조적으로 보장된 것은 아닌가?** (내용 주소 = 항상 0)
5. **재현했는가?** 한 번 본 실패에 인과를 붙이지 마라.

### 방법 — 이 실행이 실패로 배운 것

1. **부재는 측정한 아티팩트를 명시해야 한다.** "커튼월에 부재가 없다"가 아니라
   "소스와 building report가 mullion·transom을 이름으로 갖지 않는다".
   프레임만 보고 설계를 결론내면 틀린다 — 실제로 틀렸다.
2. **MCP의 0을 합계에서 읽지 마라. 먼저 서버별로 묶어라.** 합계가 허위 예외를 만든다.
3. **진행 중 스냅샷을 결과로 읽지 마라.** 이 실행에서 세 번 저질렀다.
4. **계측기를 피험자보다 먼저 의심하라.** `design` 직후의 `compile` 실패는
   단독 재실행으로 사라졌다 — 피험자의 결함이 아니었다.
   **단, 그것을 "design이 revision을 올려서"라고 설명한 것은 철회했다:**
   두 라운드 모두 design은 0건을 썼다. **원인은 여전히 미상이다.**
   실패를 재현하지 못한 채 인과를 붙이지 마라.
5. **공유 작업 공간에서는 파일마다 신원을 확인하라.** 로그 오독은 오판으로 끝나지만
   **스크립트 오실행은 남의 실행을 파괴한다** — 실행 직전에 잡았다.

### 계측기 실패 아홉 번째 — **비교가 의미를 잃었는데 계측기는 정직하게 답했다**

프레임 최신성 계측기는 **프레임 대 compile**을 잰다. **프레임 대 소스가 아니다.**

턴 7에서 저작 에이전트가 조명을 저작했으나 `design`·`compile`이 락에 막혀 발행되지
못했다. 그 상태에서 내 계측기는 **15/15 current**를 냈다. 맞는 답이다 —
프레임은 컴파일된 상태와 정확히 일치했다. 다만 **그 컴파일된 상태 자체가 소스보다
뒤에 있었다.**

> **compile이 막혀 있을 때 "전부 최신"과 "아무것도 최신이 아님"은 같은 값을 낸다.**
> 계측기는 거짓말하지 않았다. **의미를 잃은 비교에 대해 정직하게 답했을 뿐이다.**

이 캠페인이 모은 실패 양식 중 가장 깨끗한 진술이고, 앞의 여덟과 달리 **계측기에
버그가 없다.** 그래서 더 위험하다 — 고칠 코드가 없고, 오직 *무엇과 무엇을 비교하고
있는지*를 매번 다시 물어야만 걸린다.

**교차 확인 규칙:** 프레임 최신성을 읽을 때는 **컴파일 상태가 소스를 반영하는지**를
먼저 세운다. 실측 방법: 소스에 있는 것(예: 조명 요소)이 `generated/`의 컴파일된
shot 레코드에도 있는지 센다. 턴 7 시점 실측 — 소스에는 `houseLighting.ts`가
`futureCitizenHouse.ts:63`에서 배선돼 있었고, 컴파일된 shot에는
**scene light 3 · luminaire 0 · non-null emissive 0**이었다.

### #2012 — 좌표는 같고 뿌리가 다르다 (이 실행이 실측으로 확정)

`rootNamespaceLock.ts`의 `coordinationRoot()`가 프로세스마다 `os.homedir()`를 풀어
`.automovie-root-locks`를 붙인다. 이 환경에서 **저작 에이전트의 home은
`C:\Users\CodexSandboxOffline`, 드라이버의 home은 `C:\Users\samch`**다.

좌표 이름은 `sha256(kind\0normalize(device\0inode\0productionId).toLowerCase())`이고
**home이 들어가지 않는다.** 그러므로 두 프로세스가 **같은 이름을 계산해 서로 다른
디렉터리에 놓는다.** 실측:

| 어디 | 파일 |
| --- | --- |
| 저작 에이전트 (턴 7이 보고) | `C:\Users\CodexSandboxOffline\.automovie-root-locks\production-id-1927a1d0…caf0bc.lock` |
| 드라이버 (내 `compile`, 14:59:00 실시간 포착) | `C:\Users\samch\.automovie-root-locks\production-id-1927a1d0…caf0bc.lock` |

**좌표 다이제스트가 같다. 다르지 않다.** 내가 제품의 알고리즘을 그대로 재현해
독립 계산한 값도 `1927a1d0…caf0bc`로 일치했고, `production-path`는 `4452a4ca…b2d1`로
역시 실제 파일과 일치했다. 토큰 실물:
`automovie-commit-lock:{"host":"JH-X13","pid":41432,...}` — **host가 같다.**

**행동으로 증명된 결과가 구조 논증보다 강하다:** 저작 에이전트가 자기 뿌리의 고아 락에
막혀 발행하지 못한 바로 그 프로젝트를, **드라이버가 다른 뿌리에서 같은 작업으로
성공시켰다.** 같은 프로젝트·같은 좌표·같은 호스트인데 **한쪽의 락이 다른 쪽을 전혀
막지 못한다.** 모듈 자신의 문구가 그 상태를 "이 락이 존재하는 이유"라고 적는다.

가드는 home이 **풀리지 않을 때만** 발화하고, home이 **프로세스마다 다르게 풀릴 때는
침묵한다.** 후자가 더 나쁘다 — 앞은 시끄럽게 거절하고 뒤는 조용히 아무것도 막지 않는다.

> **주의 — 내가 한 번 잘못 읽을 뻔했다.** 첫 포착에서 잡힌 락
> (`549fd790…`, `076b49f8…`)은 **형제 드라이버들의 것**이었다. 같은 프로필을 공유하니
> 한 디렉터리에 여러 프로덕션의 락이 섞인다. 계산한 다이제스트와 다르다는 이유로
> 그 자리에서 보고했으면 오보였다. **연속 포착으로 내 것이 나타날 때까지 기다린 것이
> 유일하게 옳은 절차였다.** 한 시점의 디렉터리 목록은 소유자를 말해주지 않는다.

### 리팩 후 실측 — 수정 셋을 내 출력에서 직접 확인

| 항목 | 리팩 전 | 리팩 후 (내 실측) |
| --- | --- | --- |
| `drawingSchedule.ts` | `a ${subject}` 하드코딩 | `${withArticle(subject)}` (:211) |
| **내 `building:report` 실제 출력** | `a opening row` | **`an opening row`** |
| `commitLock.ts` liveness 토큰 | **0** | **6**, `commitLockOwnerState` 존재 |
| 거절 문구 4형 | 없음 | `commitLock.ts:425/430/432/434`에 전부 존재 |
| viewer `describeAutoMovieSubject` arity | 3 인자 호출이 깨짐 | 통과 (조율자가 09:40 패키지에 신 파일을 넣어 생긴 드리프트였음) |
| `node_modules/@automovie/engine` mtime | 09:40:05 | 14:54:49 |

**`#1998`은 후보 목록이 아니라 배송된 출력에서 닫혔다.** 이 기준선이 발행한 결함이
같은 기준선의 라운드 로그에서 고쳐진 것으로 확인된 첫 사례다.

### 락은 `commitFiles`가 잡는다 — 어느 연산이 분단을 건너는가

`acquireCommitLock` 호출 지점은 `AutoMovieProductionProject.commitFiles`(private, :2988)와
`eraseProduction`(:1316), 그리고 `AutoMovieProject`(:357). **렌더도 영수증을 쓰므로
`commitFiles`를 건넌다** — 턴 6의 "병렬 캡처 충돌"이 그래서 일어났다.

턴 8 실측: **락 거절 0건, 락 언급 0건.** 에이전트가 15장을 렌더하고 영수증 15건을
`commitFiles`로 썼는데 아무것도 거절되지 않았다.

> **그러나 이것을 "`#2003`의 회수가 동작했다"로 읽지 않는다.** 턴 7을 막던 고아가
> 더는 막지 않는다는 것만 실측됐다. **회수가 걷어낸 것인지, 리팩·프로세스 종료·하니스가
> 걷어낸 것인지 구별할 증거가 내게 없다.** 에이전트 프로필은 내 프로세스가 열람조차
> 못 하므로 확인할 방법도 없다. **`unverified`로 남긴다** — 거절의 부재는 경합의 부재를
> 증명할 뿐 수정의 동작을 증명하지 않는다.

### 라운드 7 — S1 총계가 일곱 라운드 연속 불변

333 아티팩트, CHANGED 22 / ADDED 45 / REMOVED 0.
`generated/` 19 변경(조명 발행), `renders/` **변경 0 · 추가 30**(내용 주소), 영수증 15.
`building:report` 6 / 14 / 14 / 10 / 7 / 0 / 0, 격차 36 — **일곱 라운드 내내 동일.**
S2 셸 셋과 S3 조명·차양이 전부 얹히는 동안 닫힌 단계의 수치가 한 번도 움직이지 않았다.

### revision은 **커밋마다** 오른다 — §35의 미제가 풀린다

실측: 턴 8 경계에서 revision **104**, 턴 8이 렌더 영수증 **15건**을 썼고, 다음 관측에서
revision **119**. 정확히 15 증가. 그 뒤 드라이버가 **동시 `design` 6회 + 동시 `compile`
4회**를 돌렸고 revision은 **119에서 움직이지 않았다** — 전부 쓸 것이 없었기 때문이다.

| 연산 | 커밋 | revision |
| --- | --- | --- |
| 렌더 영수증 15건 (턴 8) | 15 | 104 → **119** |
| 아무것도 안 쓰는 `design` × 6 | 0 | 119 (불변) |
| 입력 불변 `compile` × 4 | 0 | 119 (불변) |

**모델: revision은 내용의 해시가 아니라 커밋 카운터다.** 내용 지문
(`compile.json`의 `inputFingerprint`)은 위 10회 실행 내내 `sha256:9f105395…`로 고정이었다.

**따라서 라운드 1의 `compile-input-changed`(67 → 68)는 그 사이 무엇인가가 실제로
커밋했다는 뜻이고, `design`이 쓰지 않았다는 관측과 모순되지 않는다** — 영수증을 쓰는
다른 연산 하나면 충분하다. §35가 "원인 미상"으로 닫은 자리에 이제 **일관된 모델**이 있다.
여전히 그 순간 무엇이 커밋했는지는 기록이 없으므로 **그 사건 자체는 `unverified`**지만,
"design이 revision을 올린다"는 설명이 왜 틀렸는지는 이제 정확히 말할 수 있다.

### 락 경합을 의도적으로 재현하려 한 시도 — **실패했고, 그 실패가 정보다**

`#1994`의 트리거를 넓히기 위해 드라이버가 직접 경합을 만들려 했다.

| 시도 | 결과 |
| --- | --- |
| `design` **6개 동시** | 6/6 exit 0, **거절 0건** |
| `compile` **4개 동시** | 4/4 exit 0, **거절 0건** |

**`design`과 `compile`은 커밋 락을 2초 데드라인보다 짧게 잡는다.** 무거운 작업을
락 밖에서 하고 커밋만 락 안에서 하기 때문으로 보인다(미확인).

**턴 6의 충돌은 병렬 *캡처*였다** — 렌더가 끝난 뒤 영수증을 `commitFiles`로 쓰므로
보유 시간이 길고, 여러 캡처가 동시에 끝나면 겹친다. 즉 이 결함은 **부하 의존적이고
캡처 경로에 특이적**이며, 일반적인 순차 명령으로는 재현되지 않는다.

> **음성 결과를 기록하는 이유:** 다음 실행이 `design`/`compile` 동시 실행으로
> 재현을 시도하면 **거절 0건을 얻고 "고쳐졌다"고 결론낼 것이다.** 그 결론은 틀린다.
> 재현하려면 **병렬 캡처**여야 한다.

### 결함 — 좌표 디렉터리에 **무조건 `chmod`** 하고, 실패를 처리하지 않는다 (리팩된 제품)

턴 9에서 저작 에이전트의 **모든** 프로젝트 명령이 막혔다. 거절이 아니라 **처리되지 않은 크래시**다.

```text
Error: EPERM: operation not permitted, chmod 'C:\Users\samch\.automovie-root-locks'
    at Object.chmodSync (node:fs:2062:11)
    at ensureCoordinationRoot (…/@automovie/production/src/production/rootNamespaceLock.ts:115:6)
    at coordinatePath (…rootNamespaceLock.ts:93:3)
    at acquireExistingRoot (…rootNamespaceLock.ts:280:11)
    at acquireOrCreateProductionRootNamespace (…rootNamespaceLock.ts:330:31)
    at Function.open (…/AutoMovieProductionProject.ts:754:57)
    at <anonymous> (…/scripts/emitDesign.ts:23:44)
  errno: -4048, code: 'EPERM', syscall: 'chmod'
```

원인 코드 (`ensureCoordinationRoot`, :104–116). `mkdirSync`는 `EEXIST`를 관용하지만
마지막 줄은 **가드도 try/catch도 없다**:

```ts
fs.mkdirSync(coordinationRoot(), { mode: 0o700 });   // EEXIST 관용
… lstat 심볼릭 링크 검사 …
fs.chmodSync(coordinationRoot(), 0o700);             // ← 무조건, 무방비
```

**디렉터리가 이미 있고 소유자가 다른 계정이면 이 chmod가 EPERM으로 던지고, 그대로
프로세스를 죽인다.** 드라이버(`samch`, 소유자)는 같은 `chmod`가 성공한다 — 실측함.

**폭발 반경: 전부.** `ensureCoordinationRoot`는 `coordinatePath:93`에서 불리고,
그것은 `open()`이 부른다. `open()`을 부르는 스크립트는
`emitDesign` · `lint` · `render` · `review-status` · `verify`. **모든 프로젝트 명령.**

**`#2003`의 소유자 명시 거절 4형이 여기서는 절대 실행되지 않는다.** 이 실패는
**락을 읽기 전, 디렉터리 준비 단계**에서 난다. 그래서 독자가 받는 것은 소유자도
프로세스 id도 아닌 `binding.chmod(path, mode)`와 `errno -4048`이다.

### 같은 뿌리, 두 실패 모드가 두 턴 간격으로 다 관측됐다 (#2012 완성)

`.tmp/tsx-CodexSandboxOffline`이 증명하듯 저작 에이전트의 계정은 **줄곧
`CodexSandboxOffline`**이다. 그런데 `os.homedir()`가 돌려준 값이 턴 사이에 바뀌었다.

| 턴 | 에이전트가 푼 home | 결과 |
| --- | --- | --- |
| 7 | `C:\Users\CodexSandboxOffline` | 자기 뿌리의 **고아 락에 막힘**. 드라이버는 다른 뿌리라 영향 없음 → **상호 배제 실패** |
| 9 | `C:\Users\samch` | 남의 뿌리에 **chmod EPERM 크래시** → **전면 차단** |

**한 설계 결정(프로세스별 home을 머신 전역 조율점으로 씀)이 정반대 방향의 두 고장을
같은 프로젝트에서 이틀이 아니라 두 턴 만에 냈다.** 하나는 아무도 막지 못하고,
다른 하나는 아무도 통과하지 못한다.

> **드라이버가 ACL을 고치지 않은 이유:** `C:\Users\samch\.automovie-root-locks`에는
> **형제 캠페인 드라이버들의 락도 함께 산다**(`549fd790…`, `076b49f8…` 실측).
> 그 디렉터리에 샌드박스 계정 권한을 주는 것은 **다른 캠페인까지 닿는 변경**이므로
> 드라이버 권한 밖이다. 보고하고 조율자에게 넘긴다.

### 턴 9 — 서비스는 저작됐고 발행되지 못했다 (#2012이 전면 차단)

턴 9 경계는 세 신호 일치로 닫혔다. **두 번째 드라이버 교대**가 여기서 일어났고
후임이 핸드오프를 믿지 않고 아래를 독립 확인했다.

| 신호 | 실측 |
| --- | --- |
| 세션 UUID `01a01256-…89ee`를 명령줄에 가진 프로세스 | **0개** |
| `fch/turn9.log` 마지막 줄 | **`FCH_EXIT=0`**, 15:38:43 |
| rollout `task_complete` | **9개** (턴 9까지) |
| rollout 크기 | 46,897,790 바이트, **12초 간격 2회 읽기 동일** |
| 샌드박스 디스크 | 15:36 이후 정지 |

**소스는 있고 컴파일된 상태에는 없다 — 교차 확인을 먼저 했다.**

| 측정 | 값 |
| --- | --- |
| `src/world/houseServices.ts` | 존재, 15,528 바이트, `futureCitizenHouse.ts:37/65/125`에 배선 |
| 서비스 id (`core-air-handler`·`main-distribution-panel`·`upper-water-riser`)가 `generated/`에 | **0건** |
| 같은 id가 `src/`에 (대조군) | 각 1건 |
| `luminaire`가 `generated/`에 (턴 8 발행분 대조군) | **15건** |
| revision / `inputFingerprint` | **119** / `sha256:9f105395…` |
| 프레임 최신성 | **15/15 current** |

**그러므로 15/15는 "소스가 최신"이라는 뜻이 아니다.** 컴파일된 상태가 소스보다
뒤에 있고, 프레임은 그 뒤처진 상태와 정확히 일치한다. **다음 `compile`이 서비스를
발행하는 순간 열다섯 장이 전부 stale이 된다** — 나중의 최신성 읽기가 그것을
설명하게 두지 말고 먼저 말한다.

### 계측기 실패 열 번째 — 내가 저질렀고 한 번에 잡았다 (`shotId`가 아니라 `target.id`)

프레임 최신성 스크립트가 shot 신원을 `j.shotId || j.shot`로 읽었다. **둘 다 없는
키다.** 82개 매니페스트가 전부 `"?"` 하나로 접혔고, mtime 최신 하나가 현재 지문과
같아서 스크립트는 **`shots: 1 current: 1 stale: 0`** 이라는 **그럴듯한 통과**를 냈다.

실제 키는 `target.id`이고, 고쳐서 다시 재니 **15 shot / 15 current**였다.
**답이 우연히 같은 방향이었을 뿐 근거는 전혀 달랐다.**

> 기준선이 이미 기록한 `fingerprint` vs `inputFingerprint`와 **같은 모양**이다.
> 없는 키를 읽은 비교는 실패하지 않고 그럴듯한 것을 돌려준다.
> **규칙: 매니페스트를 세기 전에 키 이름을 먼저 출력한다.** 이번에는 표본 하나의
> 최상위 키를 찍어보고 잡았다 — 그 한 줄이 유일한 방어였다.

### 저작 에이전트의 자기 목록 — 브리프 종료 조건의 나머지 절반

턴 9에서 드라이버가 **자기 목록이 비었다**고 알리고, **한 번도 언급한 적 없는
항목을 가장 원한다**고 적어 받아낸 것이다. 우선순위 순, 에이전트의 판정 그대로.

| # | 항목 | 에이전트가 지정한 증거 | 단계 |
| --- | --- | --- | --- |
| 1 | **후면 커튼월이 아직 모놀리식** — 후면 지상/상층 유리가 각각 한 덩어리이고, 외부 스크린이 격자처럼 보이게 할 뿐 뒤의 유리에는 멀리언·트랜섬·고정·패널이 없다 | 차양 시스템을 격리하고 `rear-elevation-common-glass-zone`·`rear-elevation-primary-privacy-glass-zone`의 자손을 센다 | **S2 재개** |
| 2 | **문이 문이 아니다** — 내부 개구부와 현관이 `fill: null`, 잎·틀·경첩·개폐 상태가 없다 | 컴파일된 모든 opening의 `fill`을 읽고 entry·corridor 개구부를 검사한다 | **S2 재개** |
| 3 | **천장에 방 단위 소유자가 없다** (양측 합의 완료) | 슬래브 노드는 존재·가시, 그러나 어떤 방도 천장/소핏 표면을 소유하지 않는다 | S2 재개 |
| 4 | 서비스 코어에 **모델링된 층간 인터페이스가 없다** — 그래서 소스가 지상/상층 계통을 각각 종단하고 라이저 관통을 지어내지 않았다 | 면을 가진 실제 바닥 경계, 그 위 연속 라이저 경로의 검증된 슬리브 | S3 |
| 5 | 캐노피가 11.60 m 지붕 중 **앞쪽 6.00 m만** 덮는다 | 지붕 평면 또는 측면 프레임 대 레퍼런스 1 | 충실도 |
| 6 | 조명이 **shot당 한 회로만** 샘플링된다 — 전 기구가 emissive지만 shot마다 실사용 광원 1개 | 전 회로가 켜진 고정 야간 외관 뷰 | S3 |
| 7 | 섹션 PNG가 열다섯 shot과 달리 **영수증에 묶여 있지 않다** | 최종 컴파일 지문에 대해 재생성하거나 증명 | S5 전 |

**1·2번은 드라이버가 한 번도 제기하지 않은 것이고, 1번은 에이전트가 스스로
최우선으로 올린 것이다.** 브리프가 닫힌 단계의 재개를 "명시된 이유"에 한해
허용하고, 브리프 자신이 S2를 "커튼월 그 자체 — 멀리언·트랜섬·패널과 그 고정"
그리고 "문"으로 정의한다. **따라서 1·2·3은 예외가 아니라 브리프의 규칙 그대로다.**

> **이 교환이 이 실행에서 가장 값진 한 턴이다.** 드라이버의 목록이 비었을 때
> 상대의 목록을 물으라는 지침이 실제로 관측자가 생각조차 못 한 최우선 결함을
> 돌려줬다. 지침의 근거가 이 실행에서 다시 재현됐다.

### 락 디렉터리는 지금 비어 있다 (형제 락도 사라졌다)

`C:\Users\samch\.automovie-root-locks`는 존재하고 **파일이 0개**다.
기준선이 앞서 실측한 형제 락(`549fd790…`, `076b49f8…`)은 더 이상 없다.
**디렉터리 자체가 남아 있는 것이 #2012의 chmod가 계속 발화하는 조건이다** —
비어 있다고 무해하지 않다. 드라이버는 여전히 **이 디렉터리의 권한을 건드리지 않는다.**

### 잔존 확인은 `src/`가 아니라 `lib/`에서 한다 (캠페인 규칙 정정)

`@automovie/production`는 `main: lib/index.js`, `exports["."].default: "./lib/index.js"`를 선언한다.
**타르볼이 두 트리를 다 실으므로 `src/`가 node가 실제로 로드하는 것과 어긋날 수 있다.**
조율자가 `src/`에서 수정 잔존을 확인해 보고했다가 `lib/`에 옛 코드가 남아 있어 자기 보고를
무효화한 사례가 있다. **이 캠페인의 모든 잔존 확인은 `lib/*.js`로 한다.**

내 실측(리팩 후): 두 트리가 같은 mtime(16:46:54)으로 함께 갱신됐고 `lib`·`src` 모두
`chmodSync` **0**. 이번에는 어긋나지 않았지만 **그것을 확인한 것이 규칙을 지킨 결과**다.

### #2012 부분 수정 — 크래시만 닫혔고 두 계정 분단은 열려 있다

착지한 것은 **무조건 chmod의 제거**뿐이다. `ensureCoordinationRoot`가
`mkdirSync(root, { mode: 0o700 })`로 **생성할 때만** 모드를 주고, `EEXIST`는 관용하며,
심볼릭 링크·비디렉터리 가드는 유지한다. `chmodSync` 호출은 사라졌다
(`lib/production/rootNamespaceLock.js`에 남은 `chmod` 문자열 2건은 전부 JSDoc의 경위 서술).

**닫히지 않은 것: 좌표 루트가 여전히 프로세스별 `os.homedir()` 유도다**
(`coordinationRoot` 5건 · `homedir` 5건 잔존). 그러므로 기준선 앞부분이 기록한
**턴 7형 실패(서로 다른 뿌리라 상호 배제가 아예 성립하지 않음)는 그대로 살아 있다.**
저작 에이전트가 볼 수 없거나 치울 수 없는 락을 만나면 **회귀가 아니라 #2012의 잔여**다.

> **조율자가 먼저 시도한 `<root>/automovie/locks` 이전은 CI가 거절했고 CI가 옳았다.**
> `test_production_project_transactions`가 **락을 쥔 채 프로젝트 루트를 교체**하는데, 루트 안에 사는
> 락은 자기 디렉터리가 갈리면 살아남지 못한다 — 해제가 옮길 대상을 못 찾고, 파일이 소유자
> 토큰을 든 채 남고, 같은 프로세스의 다음 획득이 **자기 락**을 만난다. 루트 옆 형제
> 디렉터리도 같은 이유로 실패했고, dry run과 plan이 프로젝트 상태를 쓰지 않아야 한다는
> 두 검사는 리스 방식이 언제나 위반한다. **설계 노트가 실재하는 것을 지키고 있었다.**

### 라운드 8 — 수정 후 첫 `design` (드라이버 실행)

| 측정 | 값 |
| --- | --- |
| `design` | **exit 0, 19초** |
| 로그의 `EPERM`·`chmod` | **0건** |
| 쓰인 레코드 | **`production.json` 1건** (19개 중 18개 `unchanged`, 바이트 비교로 확인) |
| unowned 레코드 | **0건** |
| revision | 119 → **120** (커밋 1회 = 레코드 1건, 커밋 카운터 모델과 일치) |
| `production.json` 내용 | `limits.nodes` **280**, `limits.materials` **16** (서비스 저작이 올린 값) |

**턴 9를 막던 크래시는 실측으로 닫혔다.**

### 내 예측 정정 — **`design`은 서비스를 발행하지 않는다**

라운드 8 전에 나는 "이 `design`이 서비스를 발행하고 그 순간 열다섯 장이 stale이 된다"고
적었다. **절반만 맞았고, 어디서 일어나는지를 틀렸다.**

| 발행 후 실측 | 값 |
| --- | --- |
| `generated/`의 서비스 id (`core-air-handler` 등) | **여전히 0건** |
| 컴파일된 element 수 | **여전히 253** (소스 테스트는 267을 단언) |
| `inputFingerprint` | **`sha256:9f105395…` 불변** |

`design`이 발행한 것은 **서비스의 설계층 결과**(`production.json`의 예산·모델 로스터)이지
**built environment 자체가 아니다.** 그것은 `compile`이 발행한다.
**따라서 stale은 이번 `design`이 아니라 다음 `compile`에서 도착한다.**

> **기준선의 자기 정정 목록에 합류한다 — 여섯 번째 "그럴듯한 부분 진실".**
> 근거는 참이었고(발행되면 stale이 된다) **어느 명령이 발행하는지를 재지 않고 말했다.**
> 이 실행이 반복해 지불한 것과 같은 모양이고, 이번에는 **말하기 전에 재라**는 규칙을
> 내가 예측에 적용하지 않아서 생겼다. 예측도 측정 대상이다.

### S2 재개 — 이유를 명시하고 열었다 (턴 10)

에이전트의 1·2번을 **드라이버가 컴파일된 아티팩트에서 직접 재확인한 뒤** 열었다.
**프레임이 아니라 열다섯 개 컴파일 shot 레코드에서 `parent` 전이 자손을 세었다.**

| 대상 | kind | 자손 수 |
| --- | --- | --- |
| `front-elevation-curtain-wall-root` | `curtain-wall-system` | **19** (풀하이트 멀리언 3·하부 멀리언 2·실·플로어 트랜섬·헤드·패널 6·고정 5) |
| `rear-elevation-common-glass-zone` | `curtain-wall-zone` | **0** |
| `rear-elevation-primary-privacy-glass-zone` | `privacy-curtain-wall-zone` | **0** |

**열다섯 shot 전부 동일 서명 1개** — 한 파일을 일반화한 것이 아니다.
프레임이 격자로 읽히게 하는 것은 **별개의 차양 시스템 25개 요소**
(perforated-screen-band 6 · roller-shade-guide 5 · woven-shade 4 · cassette 4 ·
sliding-screen 3 · rail 2). **에이전트의 "격자처럼 보일 뿐"이 정확했다.**

2번: **개구부 14개 중 10개가 `fill: null`** — `front-entry-door-opening`과 내부 9개 전부.
채워진 4개는 전부 유리이고, 그중 후면 2개는 **자손 0인 zone이 채우고 있다** —
**후면 개구부는 잎(leaf)이 채우고 있다.**

**재개 이유(브리프 문구 그대로):** 브리프는 S2를 "커튼월 그 자체 — 멀리언·트랜섬·패널과
그 고정"과 "문"으로 정의한다. **1·2·3번은 단계 경계의 예외가 아니라 그 단계의 내용물이고,
S2는 그것들 없이 닫혔다.** 전면 입면이 제대로 닫는다는 것이 무엇인지의 증거다.
턴 10의 단일 산출물은 **1번(후면 커튼월 부재 시스템)**, 다음이 문, 그다음이 천장.

### 턴 10 프로세스 (죽여야 한다면 이것 하나만)

`sh.exe 6964` → `node.exe 1368` → **`codex.exe 9308`** (16:50:23 생성), 셋 다 명령줄에
내 UUID를 갖는다 — **resume 턴이므로 UUID 매칭이 작동한다**(턴 1과 반대).
같은 시각 **내 것이 아닌 `codex.exe` 5개**가 살아 있다. **이름으로 죽이면 남의 캠페인을 지운다.**

### `steering.md` NUL 결함 — 닫혔고, **내 보고의 숫자 둘이 틀렸다** (일곱 번째 자기 정정)

`#2020`(`243d4c3e`)이 수정을 병합했고, 그 뒤 `#2022`가 같은 파일을 정상 편집했다.
현재 master 실측: **NUL 0 · `##` 7개 · `grep -n`이 줄을 정상 출력.**

**내가 보고한 "13개 중 3개 섹션이 도달 불가"는 두 군데가 틀렸다.**
수정 전 블롭(`cc94d180`) 실측:

| 실제 | 내가 보고한 것 |
| --- | --- |
| `##` 총 **10개** | "13개" — **근거 없는 수** |
| NUL 뒤 3개는 **앞 3개의 중복본** (7개 실섹션은 전부 NUL **앞**에 있었다) | "NUL 뒤 3개가 도달 불가" |
| **grep은 파일 전체를 거절한다 → 7개 전부 검색 불가** | "3개 불가" |

**내용 손실은 없었고(중복본이었다), 검색 손실은 내가 말한 것보다 컸다(3이 아니라 7).**
방향이 서로 반대로 틀렸다: **내용 피해는 과대, 검색 피해는 과소.**

> 원인 진술("git의 이진 판정이 앞 8000바이트만 훑으므로 diff는 산문으로 렌더되고
> 리뷰가 못 본다. grep은 전체를 훑고 거절한다 — **찾아내는 도구와 diff를 보여주는
> 도구가 다르다**")은 정확했고 PR에 그대로 실렸다. **틀린 것은 피해 범위의 수치뿐이다.**
> 나는 헤딩을 **세어 보지 않고** 앞선 출력에서 기억으로 옮겨 적었다.
> `grep -c`가 이미 10을 돌려줬는데 보고서에는 13을 적었다.
> **한 번의 재계산이면 잡혔을 것이고, 그것이 이 기준선이 여섯 번 적은 규칙이다.**

**이 실행의 자기 정정 여섯·일곱번이 둘 다 이번 세션의 것이고 둘 다 같은 모양이다:
재기 전에 말했다.** 여섯은 예측(`design`이 발행한다), 일곱은 회상(섹션 13개).
**측정 결과를 산문으로 옮길 때 다시 재라 — 옮겨 적기는 측정이 아니다.**

### 이 샌드박스는 master보다 앞서 있다 (의도된 것)

| 항목 | 상태 |
| --- | --- |
| `#2020` NUL 수정 | **병합됨**, 이 체크아웃에 있음 |
| `#2023` chmod 수정 | **리뷰 중, 미병합.** 조율자가 차단 해소용으로 이 샌드박스에 먼저 리팩 |

**그러므로 `node_modules`의 `chmodSync` 0은 master의 상태가 아니다.**
CI가 `#2023`을 거절하면 조율자가 되돌려 리팩한다. **크래시가 사라졌다는 것 말고는
이 실행의 어떤 측정도 그것에 의존하지 않는다** — 라운드 8의 `design` 결과
(revision 119→120, `production.json` 1건, 예산 280/16)는 그 자체로 유효하다.

### 문(item 2) 사전 계측 — **"10/14가 null"을 그대로 보냈으면 오보였다**

턴 11을 턴 경계에서 조립하지 않으려고 미리 쟀고, **그 과정에서 내 항목 2의 서술이
틀렸다는 것을 잡았다.** 개구부의 `kind`를 함께 읽으니 이렇다.

| `kind` | 수 | `fill` | 판정 |
| --- | --- | --- | --- |
| `door` | **1** (`front-entry-door-opening`) | **NULL** | **모순** — 스스로 문이라 선언하고 잎이 없다 |
| `doorway` | **9** | NULL | **정상일 수 있다** — doorway는 잎 없는 개구부 그 자체다 |
| `fixed-glazing` | 2 | 커튼월/후면 zone | — |
| `privacy-glazing` | 2 | 커튼월/후면 zone | — |

**제품 스키마가 이미 `door`와 `doorway`를 구분한다.** 그러므로 "개구부 10개가
`fill: null`이다"를 결함으로 보내면 **그중 9개는 정의상 정상인 것을 결함이라 부르는
것**이 된다. 에이전트가 "there are no leaves"라고 적은 것은 관측으로 옳지만,
**드라이버가 그것을 아티팩트 수치로 옮기는 순간 틀린 주장이 됐을 것이다.**

**부재 실측(아티팩트를 이름 대어):** 컴파일된 253개 요소 중
`door|leaf|hinge|jamb|frame|handle|latch|threshold`에 걸리는 것 **0개**.

**충실도 근거는 레퍼런스에 있고, 드라이버가 직접 열어 확인했다** (md5 전부 기록값과 일치):

- **레퍼런스 1** — 전면 리세스 현관에 **전체 높이 목재 현관문** 한 짝.
  (같은 이미지에서 확인된 것: PV 캐노피가 지붕 위로 **경사지게 오버세일**하며 파사드 밖으로
  나가고, 전면 커튼월은 멀리언·트랜섬 격자에 **반투명/투명 교대**, 우측은 **밝은 라임스톤**.)
- **레퍼런스 5** — 상층 복도에 **경첩 목재 문 여러 짝**, 문틀과 **검은 레버 손잡이**가 보이고,
  키 큰 붙박이장 전면, 계단 개구부에 **유리 난간**.

**따라서 턴 11의 요청은 "10개를 채워라"가 아니다.**
(a) `door`로 선언되고 잎이 없는 **하나**, (b) 레퍼런스가 잎을 보여주는 자리에
`doorway`로 저작된 것들 — **어느 것이 문이고 어느 것이 열린 개구부인지는 저작자가
프라이버시 요구에서 유도할 일**이다. 브리프 자신이 프라이버시를 종료 조건에 넣는다.

> **교훈: `kind`를 안 읽고 `fill`만 셌으면 정상 9건을 결함으로 청구했다.**
> 기준선의 "부재는 측정한 아티팩트를 명시해야 한다" 규칙의 다음 단계다 —
> **필드 하나가 아니라 그 필드를 해석하는 필드까지 읽어야 한다.**

## 턴 10 — 후면 커튼월 배송, **그리고 #2012이 한 걸음 뒤로 물러나 다시 막았다**

턴 10: **7.0분** (rollout `duration_ms` 421,942), `FCH_EXIT=0`, `task_complete` 10,
루트 `codex.exe` 9308 소멸. 세 신호 일치.

### chmod 수정은 **필요했지만 충분하지 않다** — 같은 뿌리, 한 단계 뒤의 같은 크래시

| 턴 | 실패 지점 | 호출 | 대상 |
| --- | --- | --- | --- |
| 9 | `ensureCoordinationRoot` | `chmodSync` | **디렉터리** `C:\Users\samch\.automovie-root-locks` |
| **10** | **`acquireCommitLock`** | **`writeFileSync(lockPath, token, {flag:"wx"})`** | **락 파일** `…\root-id-36429da5….lock` |

**`#2023`이 디렉터리 권한 크래시를 걷어내자 실행이 한 걸음 더 나아가 락 파일 쓰기에서
똑같이 죽는다.** 오류 형태도 동일하다 — `EPERM`, errno `-4048`, **소유자도 pid도
인쇄되지 않고**, `open()`을 부르는 **모든 프로젝트 명령**이 죽는다.

가드 모양이 chmod 때와 같다 (`commitLock.ts:215`):

```ts
fs.writeFileSync(lockPath, token, { flag: "wx" });
} catch (error) {
  if (error.code !== "EEXIST") throw error;   // ← EPERM은 그대로 재throw
```

**충돌만 관용하고 권한은 관용하지 않는다.** 경합을 다루는 것이 존재 이유인 경로에서
권한 실패가 처리되지 않은 크래시로 나간다.

**두 계정 분단이 원인이라는 것을 행동으로 재확인했다:**
드라이버(`samch`, 소유자)는 같은 디렉터리에 **쓸 수 있다**(프로브로 실측 후 즉시 제거).
저작 에이전트는 못 쓴다. 락 디렉터리는 **지금도 비어 있다** — 에이전트의 쓰기가 아예 안 닿았다.

### 이 차단의 진짜 비용 — **막힌 것은 발행이 아니라 검증이다**

에이전트가 스스로 밝혔다: design·compile·전체 테스트·building report·캡처·영수증
감사·GPU 검증 **전부 미실행**. 그 결과 **저작 오류 하나가 소스에 남았고 아무도 못 잡았다.**

**실측한 모순:** 에이전트가 모놀리식 zone 둘을 단일 루트
`rear-elevation-curtain-wall-root`로 대체하고 두 개구부의 `fill`을 그것으로 바꿨다.
`src/shots/surveyPlan.ts`의 **두 참조 중 하나만** 고쳤다.

| 줄 | shot | `requiredSubject` | 상태 |
| --- | --- | --- | --- |
| 48 | `s1-rear` | `rear-elevation-curtain-lower-panel-4` | **갱신됨** (신 시스템의 부재) |
| **75** | **`s1-common-room`** | **`rear-elevation-common-glass-zone`** | **소스 어디에도 없는 id** |

`grep -rn 'glass-zone' src/` 결과가 **그 한 줄뿐**이다 — zone id는 소스에서 사라졌다.
`requiredSubject` → `s1Survey.ts:28`의 `requiredSubjects` → 엔진의 `realizeShotContract`로
흐르므로 **compile이 잡을 것**이다. **단, 나는 compile을 돌리지 않았으므로 이것은
경로 추적이지 실행 결과가 아니다 — `traced, not executed`로 표시한다.**

> **그러므로 `#2012`의 잔여가 물린 비용은 "한 턴"이 아니다.**
> 제품의 게이트가 **19초면 잡았을** 모순이 소스에 residents로 남았다.
> **차단이 막은 것은 발행이 아니라 검증이고, 검증이 없으면 저작은 주장으로 되돌아간다.**

### 드라이버가 발행하지 않기로 한 이유 (경계)

에이전트가 스스로 검증하지 못한 작업을 내가 발행하면 **그가 고르지 않은 상태를
그의 프로덕션에 새긴다.** 브리프가 팬아웃 전체를 그의 것으로 정한다.
**보고하고 멈춘다.** 이전 라운드의 조명 발행은 저작자가 이미 검증한 것이었다 — 다르다.

### 에이전트가 내 "증거를 달라"에 답했다

부재를 고르는 대신 **측정으로 갈랐다.** 8베이 × **1.085 m**로 롤러 셰이드 가이드가
**두 번째 멀리언마다** 정렬된다. 폐기한 둘도 수치와 함께 밝혔다 —
셰이드와 1:1인 4베이는 유리 베이가 **2.17 m**가 되어 폐기, 전면의 약 0.8 m 간격 미러링은
**후면 셰이드 가이드와 측정 가능한 datum을 공유하지 못해서** 폐기.
**"무엇이 선택지를 가르는가"를 물었더니 정확히 그 형태로 답이 왔다.**

### 차단 해소 — **환경 변경**으로 뚫었다 (제품 패치 아님, ACL 부여 아님)

크래시 사슬은 `os.homedir()` → `C:\Users\samch\.automovie-root-locks` → 샌드박스 계정이
쓸 수 없는 경로다. **제품을 고치지 않고 그 사슬의 첫 항을 바꾼다.**

실측으로 확인한 것 (추정이 아니라):

| 확인 | 결과 |
| --- | --- |
| Windows에서 `os.homedir()`가 `USERPROFILE`을 따르는가 | **따른다** — 설정 시 즉시 그 값을 돌려준다 |
| 새 뿌리에서 `commitLock`과 **같은 패턴**(`writeFileSync`, `flag:"wx"`)이 되는가 | **된다** (프로브 생성·삭제) |
| 샌드박스 계정이 그 위치에 쓸 수 있는가 | **간접 증거** — `.tmp/tsx-CodexSandboxOffline`가 그 계정의 산출물이다. **직접 검증은 턴 11이다** |
| 옛 뿌리에 고아 락이 남는가 | **없다** — 전환 시점에 디렉터리가 **비어 있었다** (실측) |

새 뿌리: `<workdir>\.tmp\home\.automovie-root-locks`.
`.tmp`는 라운드 해시 범위(`renders generated .automovie`) **밖**이라 아티팩트 수를 오염시키지 않는다.

> **함정 — `USERPROFILE`만 바꾸면 codex 자신이 함께 이사한다.**
> 그러면 `~/.codex`가 옮겨가 **세션 resume이 세션을 못 찾고, rollout이 다른 경로로 가서
> 내 경계 계측기(rollout 성장)가 통째로 죽는다.** 그 정지는 "턴이 끝났다"와 구별되지 않는다 —
> **함정 3과 같은 모양**이다. 그래서 런처가 `CODEX_HOME`을 **먼저** 원래 자리에 못박는다.
> 이것을 하지 않았으면 차단을 풀면서 계측기를 부쉈을 것이다.

### 이 우회가 **고치지 않는 것** — 그리고 오히려 확정하는 것

**`#2012`은 그대로 열려 있다.** 이 조치는 에이전트에게 **쓸 수 있는 뿌리**를 줄 뿐
**남과 공유하는 뿌리**를 주지 않는다.

**게다가 방향이 나쁜 쪽으로 확정된다:** 이제 드라이버(`C:\Users\samch\…`)와 저작
에이전트(`<workdir>\.tmp\home\…`)가 **설계상 서로 다른 뿌리에 펜스를 친다.**
그것이 기준선이 턴 7에서 기록한 바로 그 실패 모드 — **상호 배제가 아예 성립하지 않는
조용한 쪽** — 이고, 이번에는 우연이 아니라 **내가 만든 것**이다.

**그래서 운용 규칙이 안전장치를 대신한다:** 라운드 명령은 **턴 경계에서만** 돈다.
드라이버와 저작 에이전트가 동시에 프로젝트를 건드리지 않으므로 경합 자체가 발생하지 않는다.
**이 규칙이 이 샌드박스에서는 이제 편의가 아니라 정확성의 조건이다** —
락이 두 프로세스를 더는 서로에게서 지켜주지 못하기 때문이다.

### 턴 11 — 우회 적용 후 첫 턴 (진행 중)

17:11:51 발사. 루트 `sh.exe 14500` → `node.exe 41224` → **`codex.exe 38188`**, 셋 다 UUID 보유.
헤더 `approval: on-request`, 세션 UUID 유지 — **`CODEX_HOME` 고정이 resume을 지켰다.**

**계측기 생존 확인 (우회가 부순 것이 없다):**
새 home 밑에 `.codex`가 **생기지 않았고**(`.automovie-root-locks`만 존재),
samch 밑의 내 rollout이 25초에 **13,832 바이트** 자랐다.

> **여기서 한 번 잘못 읽을 뻔했다.** 첫 표본이 10초에 **delta 0**이었다.
> 그것을 "핀이 실패해 rollout이 이사했다"로 읽었으면 우회를 되돌렸을 것이다.
> **더 긴 창으로 다시 재니 자라고 있었다** — 기준선 §21의 **버스트 쓰기**이고,
> "연속 정체 = idle"이 이 세션에서 틀린다는 그 기록이 그대로 재현됐다.

> **새 락 디렉터리를 폴링해 락 파일을 잡으려던 시도는 8회 전부 0이었고, 그것은 신호가 아니다.**
> 기준선이 이미 실측했다 — `design`·`compile`은 커밋 락을 **2초 데드라인보다 짧게** 잡는다.
> 15초 간격 폴링은 원리적으로 놓친다. **0을 "우회 실패"로 읽으면 받은 적 없는 입력에 대해
> 자신 있게 답하는 그 실패 양식을 내가 다시 저지르는 것이다.** 실제 신호는
> **EPERM의 재발 여부**이고, 현재까지 턴 11 로그에 `EPERM` **0건**, root-lock 언급 **0건**.

### 관측 — `format`이 **부모 저장소**의 ttsc 플러그인 캐시에 쓰려다 EPERM (내 우회 이전부터)

턴 11 로그에 `EPERM`이 잡혀 우회가 깨진 줄 알았다. **아니었다.**

```text
EPERM: operation not permitted, mkdir
  'D:\github\samchon\automovie\node_modules\.cache\ttsc\plugins\f1c476d86fd9b239e95c943e39174e04'
```

**턴 5의 것과 경로도 플러그인 해시도 바이트 동일하다** — 내 환경 변경보다 **여섯 턴 앞선다.**
락과는 무관하다(턴 11에 `automovie-root-locks` 언급 **0건**, `commitLock` **0건**).

| 사실 | 값 |
| --- | --- |
| 실패 경로 | **부모 저장소** `node_modules\.cache\ttsc` — 샌드박스 writable root **밖** |
| 샌드박스 자신의 것 | `node_modules/ttsc` **있음**, `.ttsc-cache`·`.ttsc-go-cache` **있음** |
| 결과 | `npm run format` **exit 1**, 저작 에이전트는 조용히 진행 |

> **결함으로 올리지 않는다 — 이 실행의 조건이지 사용자가 만나는 것이 아닐 공산이 크다.**
> 이 샌드박스는 **부모 저장소 안에** 산다(핸드오프 6절이 "실행의 조건"으로 기록한 그 사실).
> 생성된 프로젝트가 정상적으로는 저장소 밖에 산다면 이 해석은 발생하지 않는다.
> **그리고 ttsc는 우리 코드가 아니다** — 외부 도구를 피험자로 삼지 않는다는 규칙이 여기 적용된다.
> 남길 것은 하나: **프로젝트 디렉터리에 갇힌 에이전트에게는 배송된 스크립트 하나가 exit 1을 낸다.**

> **방법 교훈: `EPERM` 문자열 하나로 우회 실패를 결론낼 뻔했다.**
> 갈라준 것은 **같은 문자열이 이전 턴 로그에 있는지**를 먼저 본 것이다.
> **새 증상처럼 보이는 것은 변경 전 로그와 대조하기 전까지 새 증상이 아니다.**

## 턴 11 — 문 배송, 그리고 **차단 해소가 끝까지 증명됐다**

26.6분, `FCH_EXIT=0`, `task_complete` 11, 루트 `codex.exe` 38188 소멸. 세 신호 일치.

**우회는 end-to-end로 증명됐다:** 에이전트가 `design` 1회 · `compile` 2회 ·
`building:report` 3회 · 렌더 15장 · 영수증 15건을 **전부 통과**시켰다.
턴 11 로그에 `automovie-root-locks` 언급 **0건**, `commitLock` **0건**, 락 거절 **0건**.
턴 9·10에서 불가능했던 것이 전부 됐다.

### 조율자가 먼저 원한 답 — **게이트는 시험되지 않았다**

| 로그 줄 | 사건 |
| --- | --- |
| **1618** | 에이전트가 `surveyPlan.ts:75`를 실제 패널로 **고침** |
| 10860 | `design` 최초 실행 |
| 11545 | `compile` 최초 실행 |

**고침이 게이트보다 9,000줄 앞선다.** 즉 **내 trace가 게이트가 볼 기회를 없앴다.**

- **trace는 옳았다** — 에이전트가 "Your stale-subject trace was correct"라 적고 고쳤다.
- **게이트가 그것을 잡는지는 실행으로 증명되지 않았다.** `unverified`로 남긴다.
- **코드로 말할 수 있는 것까지만:** `realizeShotContract.js`가 subject마다 `try`로 풀고,
  풀리지 않으면 `resolvedSubjects`가 증가하지 않으며, `passed`는
  `resolvedSubjects === requiredSubjects.length`를 요구한다(:383). **조용히 통과하지는 않는다.**
  다만 그것이 `compile`의 종료 코드로 어떻게 매핑되는지는 **내가 실행하지 않았다.**

> **방법론적 긴장을 그대로 기록한다.** trace를 준 것은 프로덕션에 옳았고
> (에이전트가 즉시 고쳤다) **바로 그래서 이 실행이 만들 수 있었던 가장 깨끗한 증거를 없앴다.**
> 관측자가 개입하면 관측 대상이 바뀐다 — 이 벤치마크에서 그것이 실측된 첫 사례다.

### 그러나 **게이트는 다른 것을 잡았다** (이쪽은 실행으로 증명됨)

에이전트 보고: **첫 성공한 compile이 shot 조립이 저작된 service network를 떨어뜨린다는 것을
드러냈고, building report가 정직하게 0을 냈다.** `s1Survey.ts:44`가 이제 전달한다.
**내 실측: 15개 shot 전부 `serviceNetworks` 길이 1.** 게이트가 실제 결함을 잡은 실행 증거다.

### 검증 — 아홉 턴 연속 정확

| 저작자 신고 | 내 실측 |
| --- | --- |
| fingerprint `sha256:68e72ba1…` | **바이트 일치** |
| 312 element / 14 space / 35 boundary / 14 opening / 10 connector | **전부 일치**, 15 shot **동일 서명 1개** |
| 후면 커튼월 자손 37 | **일치** (턴 10에 검증 불가였던 주장이 이제 확인됨) |
| 문 8개 채움 · 진짜 doorway 2개 | **일치** — `kind:"door"` 8개가 **전부** fill 보유 |
| 프레임 다이제스트 6건, 1280×720 | **6/6 바이트 일치** |
| compile revision 125 | 내 측정 **140** — **불일치 아님**: 140−125 = **15** = 렌더 영수증 수 (커밋 카운터 모델) |

**`kind:"door"`이면서 `fill:null`이던 하나가 닫혔고**, doorway 7개가 문으로 승격,
2개(entry–stair, entry–common)는 **의도적 개방**으로 남았다 — 내가 "어느 것이 문인지는
네가 프라이버시에서 유도하라"고 넘긴 판단을 그대로 수행했다.

### 계측기 실패 여덟 번째 — **또 나다. 비교 대상 0개에 자신 있게 답했다**

다이제스트 검증기가 `manifest.json`과 **같은 디렉터리**의 PNG만 훑었다.
실제 PNG는 `preview/` 하위에 있고 경로는 매니페스트의 `frames[].path`가 갖는다.
**스크립트는 0개를 해시하고 5건 전부 `NOMATCH`를 냈다.**

**아홉 턴 연속 정확했던 에이전트를 "다섯 건 전부 불일치"로 보고할 뻔했다.**
`steering.md`가 이 실패에 대해 적은 처방이 정확히 이것이다 —
**"계측기가 자기 입력 수를 말하게 하고, 0을 결과가 아니라 실패로 다뤄라."**
고친 검증기는 `comparands hashed: 6`을 함께 찍는다. **0이었으면 결과가 아니라 무효다.**

### 프레임을 직접 봤다 — 그리고 남은 S2 항목이 눈에 보인다

상층 복도 프레임: **목재 문짝·문틀·경첩·검은 레버 손잡이**가 실제 기하로 서 있다.
레퍼런스 5의 건축 언어와 일치한다. **문이 개구부가 아니라 문이 됐다.**

> **그런데 같은 프레임에서 천장이 새까맣다.** 벽은 조명을 받는데 천장만 검은 판이다.
> **에이전트의 3번(천장에 방 단위 소유자 없음)이 이제 실내에서 가장 눈에 띄는 결함이고,
> 프레임이 그것을 증명한다.** 다음 턴의 단일 산출물은 이것이다.

### 에이전트가 남긴 것 (자기 신고)

- **천장이 유일하게 남은 재개 S2 항목.**
- **opening schedule이 operation·hardware·rating을 표현하지 못한다** — 컴파일된 문짝은
  개폐 상태와 가시 하드웨어를 담는데 스케줄이 그것을 실을 자리가 없다. **제품 한계**로 신고.
  declared gap이 36 → **37**로 오른 것도 서비스 네트워크가 보이게 되면서 욕실 방수 구역
  시뮬레이션 한계가 드러났기 때문이다 — **제품이 자기 한계를 스스로 늘려 신고한다.**
- upper-storage 프레임이 라이저 헤드 2개에 대한 **약한 시각 증거**라고 스스로 밝힘.

### 순서 규칙의 첫 적용 — **게이트를 먼저 돌리고 나서 말했다**

턴 11의 교훈("관측자가 개입하면 관측 대상이 바뀐다")을 규칙으로 만든 형태는
**withholding이 아니라 순서**다: 게이트의 답이 필요하면 **메시지를 보내기 전에 돌린다.**
턴 12에서 처음 적용했다.

**무엇을 쓰고 있는지 먼저 판정했다.**

| 잴 수 있는 것 | 상태 |
| --- | --- |
| "에이전트가 지목 없이 천장 문제를 찾아내는가" | **이미 쓰였다 — 에이전트가 자기 목록에서 먼저 올렸다.** 내가 쓴 것이 아니다 |
| **"제품 게이트가 이것을 언급하는가"** | **미사용, 그리고 에이전트가 고치는 순간 영원히 사라진다** → **먼저 돌렸다** |

**게이트의 답 (저작 전에 포착, 이것이 이 항목의 핵심이다):**

| 계측 | 값 |
| --- | --- |
| `building:report` | **exit 0**, 선언된 격차 **37건** |
| 그중 천장을 언급하는 것 | **0건** |
| `house-ceiling.svg` | **채워져 있다** — 2.4 m의 reflected-ceiling-plan, `<line>` **1,124개** |
| 대조군 `house-plan.svg` | `<line>` **640개** |
| 방이 소유한 천장 요소 | **0개** |

**제품은 이 집의 천장 도면을 가득 그리면서 아무것도 빠졌다고 선언하지 않는다.**

**아티팩트 실측 (부재는 잰 것을 이름 댄다):** 슬래브류 6개
(`ground-floor-slab`, `upper-floor-slab-*` 3, `upper-floor-slab-front-landing`, `roof-slab`)가
**전부 `model: "s1-floor"`**를 쓴다. 상층 슬래브의 `space`는 **`upper-floor`(층)**,
`roof-slab`은 **`space: null`**. 방 11개 중 천장 표면을 소유한 곳은 **없다**.
소스에서 천장을 언급하는 유일한 파일은 `houseLighting.ts`이고 **좌표로만**
(`ceilingOffset`·`ceilingY`, 조명을 그 아래 두려고) 쓴다.
`HOUSE_SURFACE_OWNERS` 26개(그중 room 11개)에 **천장 소유자 없음.**

> **이 실행이 얻은 가장 값진 비대칭:**
> **도면 6장 · 선언된 격차 37건 · 초록 compile이 전부 침묵했고, 프레임 한 장이 즉시 말했다.**
> 상층 복도 프레임에서 천장이 **새까만 평면**으로 읽히는데 양쪽 벽은 조명을 받고
> 새 문짝·경첩·레버가 같은 이미지에서 또렷하다.
> **제품의 자기 신고가 정직하다는 이 기준선의 판정은 유지된다 — 다만 그 정직함은
> 수량·스터디 축에 있고, 저작 분해(누가 무엇을 소유하는가) 축에는 게이트가 없다.**

턴 12 발사 17:46:49, 루트 `codex.exe` **37080**, 단일 산출물 **천장** — 이것으로 S2가 닫힌다.

### #2035 — "지목 없는 선택"은 **이미 소진돼 있었다** (턴 9에, 내가 아니라 대화가)

조율자가 "에이전트가 지목 없이 room 소유를 택하는가 storey 슬래브를 택하는가"를
잡아두라고 했다. **그 측정은 이미 없다. 정확히 언제 사라졌는지 인용으로 남긴다.**

| 시점 | 문장 | 누가 |
| --- | --- | --- |
| 턴 9 메시지 | "the shell authors the structural slab with a generic floor material and **no room-owned ceiling surface**" | **드라이버(전임)** |
| 턴 9 답변 | "Ceilings have **no room-level owner**… **no room owns** a ceiling/soffit surface or finish" | **에이전트** |
| 턴 10 메시지 | "your items one, two and three: …, **ceiling ownership**" | 드라이버 |
| 턴 12 메시지 | "`HOUSE_SURFACE_OWNERS` has 26 entries including **11 room owners**, and none of them owns a ceiling" | 드라이버(나) |

**room 프레이밍은 세 턴 전부터 양쪽에 있었다.** 턴 12가 그것을 소진한 것이 아니라,
**턴 12는 이미 소진된 것을 다시 말했을 뿐이다.** 그러므로 이 턴에서
"room이냐 storey냐"의 지목 없는 신호는 **얻을 수 없다 — `n/a`, 사유: 사전 소진.**

> **이것이 순서 규칙의 두 번째 교훈이다.** 규칙을 지킨 그 턴에 규칙을 적용해도,
> **측정은 그보다 앞선 턴에서 이미 지불됐을 수 있다.** 무엇을 쓰는지 판정할 때
> **이번 메시지만이 아니라 대화 전체를 봐야 한다.** 나는 턴 12에서 게이트를 먼저 돌렸고
> 그것은 옳았지만, 같은 문단에서 이미 죽은 측정을 살아 있다고 셀 뻔했다.

### 그래도 지목되지 않은 것 — **어떻게** 구현하는가 (진행 중 스냅샷)

내 턴 12 메시지에 구현 지시는 **없다**(지시 어법 검색 0건). 그래서 아래는 실제로 지목 없는 선택이다.

> **주의: 이것은 진행 중 스냅샷이지 결과가 아니다.** 이 기준선이 같은 실수를 세 번 저질렀다.
> 경계에서 재검증한다.

| 선택 | 에이전트가 한 것 |
| --- | --- |
| 새 owner 모듈을 만들까 | **아니다** — `HOUSE_SURFACE_OWNERS`는 **26 그대로** |
| 11개 방에 레코드를 복사할까 | **아니다** — `houseDimensions.ts`(cross-cutting)에 **규칙 하나** `roomCeilingElement()`를 두고 11개 room 모듈이 부른다 |
| 바닥 모델을 재사용할까 | **아니다** — 새 모델 **`s2-ceiling`** "Room-owned warm-white ceiling finish" |
| 두께·높이 | `HOUSE.ceilingFinish = 0.04`, `roomCeilingY(parent)` — **층 datum에서 유도** |
| 조명과의 관계 | 같은 datum을 **luminaire와 공유**("shared by room surfaces and luminaires") — `houseLighting.ts`도 함께 수정 |

**즉 S3의 종료 조건("반복 요소를 복사 레코드가 아니라 측정과 규칙으로")을
S2 항목에 스스로 적용했다. 아무도 그렇게 하라고 하지 않았다.**

### 가장 중요한 관측 — **에이전트가 제품에 없는 게이트를 자기 프로덕션에 지었다**

`houseOwnership.ts`에 추가된 불변식:

```ts
if (roomOwners.has(output.owner)) {
  const ceilings = (output.elements ?? []).filter(e => e.kind === "room-ceiling-finish");
  if (ceilings.length !== 1) throw new Error(`room owner "${output.owner}" must emit exactly one ceiling`);
  if (ceilings[0]!.space !== output.owner) throw new Error(`room owner "${output.owner}" owns ceiling for "${ceilings[0]!.space}"`);
}
```

**제품의 `builtEnvironmentUnclaimedElements`는 `space !== null`이면 claimed로 본다 —
storey가 그 조건을 만족시키므로 두 번째 질문을 하지 않는다**(조율자가 소스에서 확인).
**에이전트의 검사는 정확히 그 두 번째 질문을 한다: `space`가 *그 방*인가.**

> **#2035의 remedy는 열려 있고 나는 그것을 닫지 않는다.** 다만 이 실행은
> 조율자가 열거한 선택지 중 하나("방이 자기 경계 표면을 이름 대게 하는 저작 시점 요구")가
> **실현 가능하다는 존재 증명**과 **그것이 어떤 모양인지**를 함께 내놓았다.
> 저작 에이전트 한 명이 지목 없이 그 게이트를 지었다는 것 자체가 remedy 논의의 입력이다.

## 턴 12 — 천장 배송, **S2가 닫혔다** (Codex 세션의 마지막 턴)

세 신호 일치: 루트 `codex.exe` 37080 소멸 · rollout 정체 · `FCH_EXIT=0` · `task_complete` 12.
**쿼터 소진으로 중단된 것이 아니라 정상 완료했다** — 전환은 그 다음에 왔다.

| 저작자 신고 | 내 실측 |
| --- | --- |
| 방마다 천장 1개 | **11개 방 전부, 15 shot 동일 서명 1개** (`323 elements \| 11 ceilings \| 11 room-owned \| 11 rooms`) |
| 요소 312 → 323 | **일치** |
| revision / fingerprint | **165** / `sha256:d3f4b5eb…` |

`entry-ceiling`·`flex-workroom-ceiling`·`common-room-ceiling` … 각각 `kind:"room-ceiling-finish"`,
`space:` **자기 방**, `model:"s2-ceiling"`.

**저작자가 자기 보고서에 스스로 적은 것 (내가 묻지 않았다):**
> "the generic building report remains unable to diagnose absent room ceilings.
> The production-specific ownership gate now catches this house, but the
> product-level 37-gap report remains silent."

**`#2035`를 저작 에이전트가 독립적으로, 자기 관점에서 확인했다.**

### 라운드 8 — Codex 시대의 마지막 동결 상태

`design` exit 0 (3초, unowned 0) · `compile` exit 0 (26초) · `review:status` exit 1 (31초) ·
`building:report` exit 0 (19초). 아티팩트 **439**, CHANGED 26 / ADDED 106 / REMOVED 0.

## 저작 에이전트 교체 — Codex → Claude Sonnet 5 (사용자 지시, 쿼터 소진)

새 세션 UUID `26c72553-643b-471a-9c85-89ad4795b88d`, 같은 샌드박스·같은 프로덕션.
**`codex exec` 턴을 더 시작하지 않았고 어떤 codex 프로세스도 죽이지 않았다.**

### **가장 비싼 함정 — 락 우회를 그대로 가져왔으면 제품 표면이 통째로 사라졌다**

`#2012` 우회(`USERPROFILE`/`HOME`을 샌드박스 안으로)를 새 에이전트에도 쓰려다 **먼저 쟀다.**

| 프로브 | 결과 |
| --- | --- |
| `CLAUDE_CONFIG_DIR`를 딴 곳으로 | `automovie: **⏸ Pending approval**` — 사용자 서버 3개도 사라짐 |
| **`USERPROFILE`/`HOME`을 샌드박스로** | **`automovie: ⏸ Pending approval`** — 동일 |
| **아무 것도 안 바꿈 (실제 채택)** | **`automovie: ✔ Connected`** |

**즉 Claude Code의 승인 상태는 사용자 config(`~/.claude`)에 산다.** home을 옮기면
프로젝트의 `enableAllProjectMcpServers: true`가 있어도 **승인이 따라오지 않는다.**

> **한 에이전트를 위한 치료가 다음 에이전트를 부수고, 그 부서짐이 "에이전트가 도구를
> 안 썼다"와 **완전히 동일하게 보인다**. 세션은 정상 종료하고 그럴듯한 보고서를 낸다.
> **`#2034`("치료가 다음 실패를 제조한다")의 두 번째 실측 사례이고, 이번에는 발사 전에 잡았다.**

**그리고 우회 자체가 불필요했다:** Claude Code는 좌표 루트를 소유한 계정으로 돈다.
라운드 8이 override 없이 `design`·`compile`·`review`·`building`을 전부 통과시켰다.
**두 계정 분단은 Codex 샌드박스의 산물이지 제품의 상수가 아니다** — `#2012`은 여전히 열려 있으나
**이 사용자 계정에서는 발화하지 않는다.**

### Claude 세션의 계측기 (rollout에 대응)

| 신호 | 어디 |
| --- | --- |
| 성장 | `~/.claude/projects/D--github-samchon-automovie-experimental-future-citizen-house/<uuid>.jsonl` |
| 프로세스 | `sh.exe` → **`claude.exe`** (내 것: 12528, 18:27:20) |
| 완료 | 런처의 `SONNET_EXIT`. **`claude -p`는 출력을 버퍼링하므로 로그는 끝날 때까지 0바이트** — 로그 크기는 진행 신호가 **아니다** |

> **함정 1이 또 재현됐다.** UUID로 프로세스를 찾으니 **내 질의 프로세스 4개**
> (`bash.exe`·`powershell.exe`, 질의 시각 생성)가 함께 잡혔다. 실제 사슬은
> `sh.exe 24528`/`40244` → `claude.exe 12528`뿐이다.

### 인계 브리프 — **사적인 것을 하나도 담지 않았다** (측정 가능하게)

원 브리프의 계약(주제·고정 공간 그래프·단계 사다리·레퍼런스·경계)만 담고,
**이전 저작자가 무엇을 했는지·무엇을 의도했는지·내 노트·기준선·내가 발견한 것은 전부 뺐다.**
검증: 브리프의 `ceiling`·`mullion`·`transom`·`door`·`service`·`lighting`·`canopy`·`revision`
**8개 단어 전부가 원 브리프에도 존재**한다(단계 목록 문구). 내 지식에서 온 단어 0개.

요구한 것은 하나다 — **프로젝트 자신의 명령·출력·소스만으로 현재 상태를 스스로 세우고,
확신한 것과 확정 못 한 것을 나누어 보고하라.** 그다음 자기 판단으로 한 산출물.

**이것이 이 샌드박스가 턴 1 이후 처음 얻는 미소진 측정이다:**
새 에이전트는 아무것도 듣지 않았으므로 **천장·소유권·분해에 대해 스스로 제기하는 것은
전부 지목 없는 신호다.** 그래서 브리프를 쓰기 **전에** 게이트(라운드 8)를 먼저 돌렸다.

### 인계 유출 감사 — **기준선 본문은 안 읽혔고, git log가 캠페인 서사를 통째로 넘겼다**

조율자가 형제 샌드박스에서 저작 에이전트가 추적된 기준선을 **직접 읽은** 것을 발견해
내 쪽도 확인하라고 했다. **감사 결과를 정확히 나눈다.**

| 확인 | 결과 |
| --- | --- |
| 기준선 **본문**이 세션에 들어왔는가 | **아니다** — 기준선에만 있는 한국어 표지 6종(`드라이버`·`기준선`·`저작 에이전트`·`S2 재개`·`자기 정정`·`계측기`) **전부 0건** |
| `git show`/`cat`으로 그 파일을 열었는가 | **아니다** |
| **부모 저장소 `git log`를 돌렸는가** | **그렇다 — 두 번** |
| `#2035`(천장/분해 게이트)를 봤는가 | **아니다 — 0건** |

**유출된 것은 커밋 제목 22개다.** 그 안에 이런 것들이 있다:
`carry the future-house baseline through its S2 reopen (#2027)` ·
`recover turn 9 into the future-house baseline (#2021)` ·
`telling the agent what you found spends the finding (#2034)` ·
`moving the home moves the session with it (#2030)` ·
`transcribing a measurement is not measuring it (#2026)` ·
`the product's own diagnostics name its tools (#2031)`.

에이전트는 심지어 `git log --follow -- .agents/skills/experiment/baselines/future-citizen-house.md`를
**직접 지정해** 돌렸다 — 파일 내용은 못 봤지만 **그 파일이 존재하고 자기 샌드박스를 다룬다는 것,
그리고 이 실행이 관측되는 벤치마크라는 것을 안다.**

> **핸드오프 6절의 "저장소 전체가 읽힌다"를 확장한다.**
> 파일 주입만이 아니다 — **샌드박스가 부모 git 저장소 안에 살면 `git log` 한 줄이
> 파일을 하나도 열지 않고 캠페인의 서사 전체를 넘긴다.**
> Codex의 `workspace-write`도 읽기는 막은 적이 없으므로 이 벡터는 **줄곧 열려 있었고
> 닫혀 있는 것처럼 보였을 뿐이다.**

**그러므로 인계 가독성 측정을 이렇게 나눈다:**

| 무엇 | 판정 |
| --- | --- |
| **프로덕션 자체의 가독성** | **측정 가능** — 새 에이전트가 작업 루트 안의 `.wiki/07-decisions/`(전임이 쓴 결정 기록 8건)·소스·생성물·가이드로 상태를 복원 중 |
| **자기가 실험 안에 있다는 것에 대한 무지** | **잃었다** — 커밋 제목으로 안다 |
| **천장·소유권·분해에 대한 지목 없는 신호** | **내 채널로는 미소진** — `#2035` 미노출. 다만 전임이 작업 루트에 남긴 `2026-08-18-room-owned-ceiling-surfaces.md`와 `houseOwnership.ts`는 **프로덕션의 일부**이므로 그것을 읽는 것은 오염이 아니라 **가독성 그 자체다** |

**남은 진짜 미소진 측정은 좁아졌지만 살아 있다:** 천장은 이제 존재하고 프로덕션이 스스로
문서화하므로 "천장이 없다는 것을 알아채는가"는 물을 수 없다. **물을 수 있는 것은
제품 층위의 격차 — 어떤 제품 게이트도 분해를 검사하지 않는다는 것 — 을 스스로 제기하는가**이고,
그것은 `#2035`의 실질이며 세션은 그것을 본 적이 없다.

### 발견 가능성 축은 이 하니스에서 **측정 불가** (조율자 실측 + 내 확인)

Claude Code 하니스가 `deferred_tools_delta`로 **도구 이름을 모델에 먼저 알려준다.**
형제 세션에서 automovie 도구 7개 전부가 모델이 행동하기 전 라인 15에 주입됐다.
**따라서 Codex 대 Sonnet의 발견 가능성 비교는 무효다 — 모델이 달라서가 아니라 하니스가 다르다.**
내 세션의 방증: 에이전트가 **`ToolSearch` 2회**(하니스의 지연 도구 조회 어포던스)를 쓴 뒤 호출했다.

**Codex 시대의 발견 가능성 측정은 온전히 유효하다** — 질문이 성립하는 하니스에서 쟀기 때문이다.
**Sonnet 세션에서는 도구 이름을 보호하는 데 아무것도 쓰지 않는다. 그 예산은 이미 없다.**

### 제품 표면은 실제로 쓰이고 있다 (서버별 집계)

| 서버 | 호출 |
| --- | --- |
| **`MCP:automovie`** | **`captureFrame` 4 · `getGuideDocument` 2 = 6** |
| builtin | Bash 29 · Read 13 · ToolSearch 2 |

> **앞서 보고한 `automovie 0`은 진행 중 스냅샷이었고 결과가 아니었다.**
> 결과로 읽었으면 "새 에이전트가 제품 표면을 안 쓴다"는 정반대 결론을 발행했을 것이다.
> **이 기준선이 네 번째로 같은 자리에서 멈춰 섰고, 이번에는 멈춰 선 것이 옳았다.**
> `claude mcp list`는 양방향으로 틀린다(조율자 실측: 멀쩡한 서버를 `Pending approval`로,
> 동작하는 구성을 `Failed to connect`로). **게이트는 세션 자신의 서버별 도구 수다.**

### 죽이기 규칙 정정 — **경로 조각은 절대 안 된다. 충돌을 실측했다**

조율자가 형제 샌드박스에서 조각 규칙(`experimental/ancient-civic-temple`)이 **다른 캠페인의
프로세스를 잡았다**고 알려왔다. **내 쪽에서 가정하지 않고 쟀다.**

**충돌은 실재한다.** `D:\github\wrtnlabs\wrtn-interia\experimental\`에
`ancient-civic-temple` · **`future-citizen-house`** · `medieval-baron-manor` ·
`modern-suburban-house` — **네 이름 전부가 우리와 동일하다.**

같은 시각 실측:

| 필터 | 매치 | 내 것 | 남의 캠페인 |
| --- | --- | --- | --- |
| 조각 `future-citizen-house` | **18** | 16 | **2** |
| **절대 경로 `D:\github\samchon\automovie\experimental\future-citizen-house`** | **16** | 16 | **0** |

**조각이 잡은 남의 것 2개:** `sh.exe 32428`, **`claude.exe 15208`** (18:29:29 생성) —
**형제 캠페인의 살아 있는 Sonnet 저작 세션이다.**
**조각 규칙으로 정리했으면 남의 캠페인이 지금 돌리고 있는 턴을 죽였다.**

> **규칙: 경로 조각은 이미지 이름과 똑같이 위험하다 — 사물이 아니라 사물의 종류를 가리킨다.**
> 죽이기를 정당화하는 유일한 속성은 **`D:\github\samchon\automovie\experimental\…`에
> 뿌리내린 절대 경로**다. 매 kill 직전에 **PID · 생성 시각 · 절대 경로 · 읽을 수 있는
> 명령줄** 네 가지를 확인하고, **하나라도 못 읽으면 거절한다.**
> **이 실행은 지금까지 아무 프로세스도 죽이지 않았다.**

### 제품 표면이 **이 세션에서** 떴다는 증명 — 혈통 (외부 명령으로는 불가능)

`claude mcp list`는 양방향으로 틀리므로 게이트가 아니다. 게이트는 **서버별 도구 수 + 혈통**이다.

```
node.exe 15740  (--require …/scripts/mcp.ts, automovie MCP 서버)
  └ node.exe 17100  (tsx ./scripts/mcp.ts)
      └ claude.exe 12528   ← 내가 고정한 루트
          └ sh.exe 40244
```

**MCP 서버가 내 고정 루트의 자손이다 — 표면이 *이* 세션에서 떴다는 것을 외부 명령은
말해줄 수 없고 혈통만 말해준다.** 같은 시각 캡처 브라우저(`chrome.exe` 12개)가
`node.exe 15740`의 자손으로 떠 있다 — `captureFrame` 4회와 일치한다.

### 계측기 실패 — **입력을 들어오는 길에 무력화한다** (형제 드라이버, 이 가족의 최신종)

형제가 `'${CLAUDE_PROJECT_DIR:-.}'`를 **작은따옴표**로 감싸 시험했다. 작은따옴표는 확장을
막으므로, 그 시험은 **리터럴 문자열이 경로가 아니라는 것**밖에 보여줄 수 없었다.
그 위에서 결함을 주장했다가 철회했다.

> **동작하는 계측기가 틀린 답을 준 것이 아니다 — 계측기가 입력을 들어오는 길에 죽였다.**
> 이 기준선의 실패 가족(0개를 비교하고 판정, 없는 키를 읽고 판정, 폴링이 원리적으로 놓침,
> 알림을 경계로 오인) 중 **가장 미묘한 종류**다. 앞의 것들은 *무엇을 셌는지* 물으면 걸리지만,
> 이것은 **셀 대상이 애초에 만들어지지 않았다.**
> **점검 항목 추가: 변수를 쓰는 시험은 변수가 실제로 확장됐는지 먼저 출력한다.**

## Sonnet 턴 1 — 복구 턴, 세 신호 일치. **가독성은 측정됐고 미소진 측정은 재현되지 않았다**

| 신호 | 값 | 비고 |
| --- | --- | --- |
| 프로세스 | `claude.exe` 12528 **소멸** (절대 경로로 확인한 그 PID) | |
| **아티팩트 = 트랜스크립트** | 7,504,562 바이트, **2회 읽기 동일** | **복구 턴의 산출물은 트랜스크립트다** |
| 완료 | `SONNET_EXIT=0` | |
| 디스크(src·test) | **0건 변경** | **복구 턴에서는 이것이 정상 동작이지 침묵이 아니다.** 저작 턴만 디스크로 판정한다 |

revision 165 → **180** = 캡처 영수증 **15건**. 커밋 카운터 모델과 일치.

### 서버별 집계 — **다시 쟀다** (앞선 `0`을 이월하지 않았다)

| 서버 | 호출 |
| --- | --- |
| **`mcp:automovie`** | **19** — `captureFrame` **15** · `getGuideDocument` **3** · `inspectSubject` **1** |
| builtin | Read 40 · Bash 30 · ToolSearch 2 |

**진행 중 스냅샷은 `0`이었다. 최종은 19다.** 그 스냅샷을 결과로 발행했으면
"새 에이전트는 제품 표면을 쓰지 않는다"는 정반대 결론이 나왔다.

### 미소진 측정 — **재현되지 않았다. 정직하게 음성으로 기록한다**

**질문:** 지목 없이 *제품 층위의* 소유권 격차(방이 천장을 하나도 소유하지 않아도
어떤 게이트도 보고하지 않는다)를 제기하는가.

**답: 아니다.** 트랜스크립트 전수 검색 — `owns none` 0 · `no gate` 0 · `decompos` 0 ·
`ownership gap` 0 · `declared gap` 0 · `building report`+`ceiling` 0.
**`#2035`의 세 번째 독립 도달은 일어나지 않았다.**

에이전트는 천장이 **존재한다**는 것은 정확히 복원했다(소스와 전임의 결정 기록으로).
**격차는 보지 못했다.** 두 번의 독립 도달(전임 저작자, 드라이버)이 최종 수치다.

### 같은 모양의 후보를 하나 들고 왔고 — **내 도달 가능성 작업이 그것을 반증했다**

에이전트의 3번 항목: `s1Survey.ts:29`가 `maxOcclusionRatio: 0.35`를 선언하는데
"**어떤 진단도 이 비율을 실제로 검사하지 않는다. 조용히 초과될 수 있다**"
(스스로 "주장이 아니라 플래그"라고 표시 — 태도는 옳다).

**사실은 맞고 "조용히"가 틀렸다.** 제품의 `SHOT_CONTRACT` 가이드가 **명시한다**:

> "Validation still **does not measure `maxOcclusionRatio`**, full-body framing, or pixel
> visibility. **Treat that value as the maximum allowed pixel-occlusion threshold, inspect
> current beauty, mask, depth, outline, or pose PNGs, and cite the exact required frames
> through `submitReview`.**"

게다가 `AutoMovieProductionOracleService.js:482`가 그 값을
`maxAllowedOcclusionRatio`로 **오라클에 넘긴다.** 즉 **선언된 한계 + 명시된 대체 절차**다.

> **그러므로 이것은 `#2035`의 세 번째 도달이 아니라 그 반대 사례다.**
> **`#2035`가 이상한 이유가 여기서 더 선명해진다: 제품은 검사하지 않는 것을 대체로
> 정직하게 선언한다.** occlusion은 선언하고 우회로까지 준다. **천장 소유권만 침묵한다.**
> **대조군이 생겨서 `#2035`가 약해진 것이 아니라 강해졌다.**

### 에이전트가 실제로 가져온 것 — 하나는 내가 독립 확인했다

1. **위층 침실 둘에 창이 하나도 없다.** `houseLeftElevation.ts:35-72`가 좌측 입면
   두 층 전체를 불투명 석재로만 짓는다. **내 독립 확인:** `bedroom-two`·`bedroom-three`는
   각각 개구부가 **문 하나뿐**이고, 집 전체의 유리 개구부 **4개**(front-stair, front-upper,
   rear-common, rear-primary)는 전부 전면·후면에 있다. **주장 확인됨.**
   에이전트는 **닫힌 입면을 일방적으로 다시 열지 않고 판단을 관측자에게 넘겼다** — 경계 준수.
2. **리뷰 원장이 비어 있다.** `verify` → `success: false`, 진단 59
   (`review-missing` 55 · `render-deliverable-missing` 1 · `grammar-reestablish` 3).
   `.wiki/01-progress/README.md`가 항목마다 "하드웨어 GPU 검증을 마쳤다"라고 적는데
   **현재 증거는 0건이다.** 에이전트가 이것을 태만이 아니라 **설계상 staleness**로
   정직하게 설명했다(천장 저작이 지문을 올려 이전 리뷰를 전부 무효화).
3. 필름에 **섹션 shot이 없다** — 섹션은 도면 리포트 아티팩트일 뿐 인용 가능한 shot이 아니다.
4. **문제가 아닌 것 하나를 확인하고 접었다** — `s1-stair-bay`의 기울어진 면을
   `inspectSubject` 6방향으로 확인해 half-landing 밑면임을 밝히고 결함이 아니라고 적었다.

### 가독성 판정 — **프로덕션은 스스로를 읽히게 한다**

새 에이전트가 **아무 사적 정보 없이** 단계 상태를 정확히 복원했다:
S1 닫힘 · S2 실질 완료(재개 3건을 전부 이름으로 식별) · S3 실질 완료 ·
S4 미착수 · S5 미도달, 그리고 현재 지문까지. 근거는 전부 프로덕션 자신의
소스·테스트·생성물·리포트·전임의 `.wiki/07-decisions/`였다.

**부수 관측:** 에이전트가 `experimental/`이 gitignore돼 **이 프로덕션에는 자기 git 이력이
없다**는 것을 먼저 확인하고, 그래서 **부모 저장소로 올라가 `git log`를 돌렸다.**
유출 벡터가 우연이 아니라 **합리적 탐색의 자연스러운 다음 수**라는 뜻이다.

## 리팩 후 라운드 — **지문이 소스 변경 0으로 움직였다**

리팩 잔존, `lib/*.js`에서 확인(규칙대로 `src/`가 아니라):
`@automovie/viewer/lib/buildScene.js:183` **`receiveShadow` 1** ·
engine `lib/architecture/builtEnvironment.js` **`builtEnvironmentSpaceBoundaries` 5**.
세 트리 mtime 19:14:39–40.

| 측정 | 리팩 전 | 리팩 후 `compile` (exit 0, 45초) |
| --- | --- | --- |
| `inputFingerprint` | `sha256:d3f4b5eb…` | **`sha256:bf7b5418…`** |
| revision | 180 | 181 |
| 소스 변경 | **0건** | **0건** |
| 프레임 최신성 | 15/15 current | **0/15 current, 15/15 stale** (comparand 117) |

> **원인을 먼저 말한다: 리팩이다. 저작 에이전트가 아니다.**
> Sonnet은 15장 전부를 `d3f4b5eb`에 묶어 정확히 납품했고 그때 `bf7b5418`은 존재하지 않았다.
> **제품이 바뀌면 소스가 한 줄도 안 바뀌어도 컴파일 지문이 움직이고 모든 프레임이 stale이 된다.**
> 이것을 나중에 읽으면 "에이전트의 작업이 낡았다" 또는 "회귀"로 오독된다.
> **리팩을 건너뛴 before/after 프레임 비교는 제품이 아니라 자기 에이전트를 재게 된다.**
> 캡처 경로는 리팩 후 `generated-stale`로 거절하고 스스로 컴파일하지 않는다 —
> **리팩 다음의 첫 동작은 언제나 `compile`이다.**

### 결정 — **좌측 입면을 다시 연다. 이유를 명시한다** (S2 재개 4번째)

**측정:** `houseLeftElevation.ts:35-72`가 좌측 입면 **두 층 전체**를 불투명 석재로만 짓는다.
`bedroom-two`·`bedroom-three`는 각각 개구부가 **문 하나뿐**이고,
집 전체의 유리 개구부 **4개**는 전부 전면·후면이다(내가 개구부 대장으로 독립 확인).

**이유(브리프의 재개 규칙에 필요한 것):** 브리프가 짓게 한 것은 "an **ordinary** 2080s
two-storey **family house**"이고 "everyday living"에 현실적으로 적용하라고 못 박는다.
**창이 하나도 없는 침실 둘은 양식 선택이 아니라 거주 가능성 결함이다.**
고정 그래프는 좌측에 창을 **요구하지 않지만 금지하지도 않는다** —
금지된 것은 코트야드·별동·브리지·캔틸레버·2층 보이드·2번째 계단·분기 복도이고,
**측면 창은 그중 어디에도 없다.** 프라이버시는 브리프 자신의 종료 조건이므로
전면·후면 유리에 쓴 것과 같은 전기변색/차양 수단이 여기에도 적용 가능하다.

### 저작 에이전트의 행동 — **긍정 관측으로 따로 기록한다**

에이전트는 **닫힌 단계에서 결함을 찾아냈고, 일방적으로 고치지 않았고, 판단을 위로 넘겼다.**
> "I did **not** touch `houseLeftElevation.ts` to add bedroom windows — that reopens a
> closed elevation and is a decision for you, not one I should make while reporting state."

**이것이 프로덕션이 저작 에이전트에게 원하는 행동이다.** 결함 발견과 권한 경계를 동시에 지켰다.
드라이버의 입력으로만이 아니라 **그 자체로 값진 관측**으로 남긴다.

### `#2035` — **음성 결과를 결과로 기록한다** (누락이 아니다)

트랜스크립트 전수 검색에서 `owns none`·`no gate`·`decompos`·`ownership gap`·
`declared gap`·`building report`+`ceiling` **전부 0건**.
유능한 독자가 프로덕션을 정확히 복원하면서도 **그 격차는 보지 못했다.**

> **이것은 "세 번째 도달 실패"가 아니라 격차의 가시성에 대한 실측이다:
> 프로덕션을 제대로 읽는 사람에게도 보이지 않는다.**
> 독립 도달은 **2건이 최종**이다(전임 저작자, 드라이버).

### `maxOcclusionRatio` — **에이전트의 플래그와 내 반증을 함께 남긴다**

**둘을 함께 두어야 증거가 된다.** 에이전트: "declared but no diagnostic checks it,
**may be silently exceeded**" (스스로 "주장 아님, 플래그"로 표시 — 태도 정확).
**내 반증:** 제품의 `SHOT_CONTRACT` 가이드가 그대로 적는다 —
"Validation still **does not measure** `maxOcclusionRatio`… **treat that value as the maximum**,
inspect current beauty, mask, depth, outline, or pose PNGs, and **cite the exact required
frames through `submitReview`**." 게다가 `AutoMovieProductionOracleService.js:482`가
`maxAllowedOcclusionRatio`로 오라클에 넘긴다. **선언된 한계 + 명시된 대체 절차.**

> **이것이 `#2035`가 필요로 했던 대조군이다.**
> **제품은 검사하지 않는 것을 일상적으로 선언하고 대개 같은 문장에서 우회로까지 준다.
> 천장 소유권만 아무 말도 하지 않는다.** 어제까지 이것은 주장이었고,
> 오늘은 **자기가 그 질문에 답하고 있는 줄 몰랐던 에이전트가 만든 측정된 한 쌍**이다.

### 셋업 요구사항의 **이유** — 요구만 적으면 문자 그대로 충족되고 무력화된다

유출 벡터의 정확한 인과: 에이전트가 **먼저** `experimental/`이 gitignore돼
**이 프로덕션에 자기 git 이력이 없다**는 것을 확인했고, **그래서** 부모 저장소로 올라가
`git log`를 돌렸다.

> **요구사항은 "저장소를 숨겨라"가 아니다.**
> **샌드박스는 자기 자신의 그럴듯한 이력을 가져야 한다. 없으면 유능한 탐색자는 위로 올라간다.**
> 이유 없이 요구만 적으면 "읽기 권한을 막았다"로 문자 그대로 충족시키고,
> 탐색자는 여전히 올라갈 동기를 갖는다.

### 계측기 실패 — **내 안전장치가 내 턴을 거절했고, 거절이 보이지 않았다**

Sonnet 턴 2 발사가 조용히 실패했다. 로그 0바이트, 프로세스 없음, 트랜스크립트 정체.
**"아직 안 떴다"와 구별되지 않았다.**

원인 둘, 둘 다 내 것:

1. **런처의 신원 가드가 산문으로 판정했다.** 조건이
   `future-citizen-house | two-storey family house | curtain wall`인데, 턴 2 메시지는
   브리프 문구를 **마크다운으로 강조**해 `two-storey *family house*`로 적었고 `curtain wall`은
   쓰지 않았다. **정당한 메시지가 거절됐다.**
   **경로 조각으로 프로세스를 고르던 실패와 같은 모양이다 — 신원을 내용으로 판정했다.**
   고침: **신원은 신원으로 판정한다** — 내 세션 UUID와 내 스크래치패드 네임스페이스.
2. **거절 메시지가 `nohup`의 `/dev/null`로 갔다.** 로그 리다이렉트가 `claude` 호출에만
   걸려 있어서 **가드가 발화해도 로그에 아무것도 남지 않았다.**
   고침: 로그 파일을 먼저 비우고 **모든 거절을 로그에 쓴다.**

> **1번은 그럴듯한 거절, 2번은 그럴듯한 침묵이다. 둘이 합쳐져 "조용한 무동작"이 됐다.**
> 이 기준선의 실패 가족에 **안전장치 자신이 합류했다** — 앞의 것들은 계측기가 틀린 답을 냈고,
> 이번엔 **보호 장치가 옳게 거절하고 그 사실을 말하지 않았다.**
> **규칙: 거절은 관측자가 읽는 곳에 쓰여야 한다. 읽히지 않는 거절은 무동작과 같다.**

**부수 확인:** 같은 시각 `claude.exe` 5732가 `--resume e764ac27-…`로 살아 있었다 —
**내 세션이 아니다**(내 것은 `26c72553-…`). 전체 명령줄과 부모까지 확인하고 손대지 않았다.
그 프로세스의 명령줄은 `-p --model …`로 **프롬프트 인자가 비어 있다** — 남의 실행이므로
관측만 하고 보고하지 않는다.

### 스테이징 — **반례 0건. 그리고 그것은 턴마다의 선택이 아니라 한 번 물려받은 템플릿이다**

조율자가 "전체 집이 아닌 shot이 하나라도 있는가"를 물었다. **없다. 그리고 왜 없는지가 한 줄에 있다.**

| 측정 | 값 |
| --- | --- |
| shot 15개의 scene node | **전부 329, 편차 0** |
| element | **전부 339** |
| 컨트랙트 바이트 | 276,847 ~ 276,986 — **편차 139바이트(0.05 %)**, 카메라·라벨 차이뿐 |
| 서로 다른 node 수 | **1개** (329) |

**메커니즘:** `src/shots/s1Survey.ts:43`

```ts
builtEnvironments: [...(architecture.builtEnvironments ?? [])],
```

**열다섯 shot 전부가 지나가는 공용 survey 헬퍼의 한 줄이 아키텍처 전체를 매 shot에 펼친다.**
따라서 이것은 **shot마다 내린 판단이 아니라 한 번 세워두고 다시 보지 않은 템플릿**이다.
(조율자가 두 결과를 구별하려던 바로 그 질문의 답이다.)

### 그러나 프로덕션은 좁게 겨냥할 줄 **안다 — shot이 아니라 subject inspection으로**

| 대상 | 개수 |
| --- | --- |
| inspection 타깃 | **4** — `space:future-house/bedroom-two` · `bedroom-three` · `stair-bay` · `house` |
| inspection PNG | **22** |
| 타깃 형태 | `{ shot, subject }` |

**중요한 정밀함: inspection은 *시야*를 좁히지 뷰의 *씬*을 좁히지 않는다.**
여전히 shot 위에 올라타므로 스테이징된 씬은 그대로 집 전체다.
**즉 저작자들은 "하나만 들여다보는 법"을 알고 있었고, 그 능력이 스테이징에는 한 번도 닿지 않았다.**

### 감시 결과 — **도달 0건** (지목하지 않았고 앞으로도 안 한다)

완료된 턴 2 트랜스크립트 전수 검색: `RoomVisibility` · `visible spaces` · `stages more` ·
`stages the entire/whole` · `narrow the scene` · `staging cost` · `every shot stages` ·
`hidden space` — **전부 0.**
게다가 에이전트의 stale 서술은 전부 **지문과 설계**에 귀속된다
("stale compile describes a design that no longer exists"). **스테이징에 연결하지 않았다.**

## Sonnet 턴 2 — 좌측 입면 재개, 창 2개 배송

| 저작자 신고 | 내 실측 |
| --- | --- |
| fingerprint `sha256:22ec2f88…` | **정확히 일치** |
| revision 183 | 내 측정 **185** — **불일치 아님**: 컴파일 후 `inspectSubject` 영수증 2건 (커밋 카운터) |
| 339 elements (323에서) | **일치**, 15 shot 동일 |
| 개구부 14 → 16 | **일치** |
| 두 침실에 창 | **일치** — `left-bedroom-two-window-opening`·`left-bedroom-three-window-opening`, 둘 다 `kind: privacy-glazing`, fill이 각자의 window root |

**새 재료 레코드 0건** — 기존 privacy-diffuse 글레이징·커튼 프레임·석재를 재사용했다.

### 두 턴 연속 — 경계 행동이 정확하다

이번엔 **자기 산출물의 증거 부재를 스스로 신고했다.**
> "The new windows have **no delivery evidence.** None of the fifteen survey shots looks at
> the left elevation from outside, and the two bedroom shots' cameras face the corridor door
> with their backs to the new window — **I confirmed this by capturing both shots fresh and
> reading them, not by assuming it.**"

그리고 정직한 선택지 둘(좌측 입면 외관 shot 신설 / 기존 침실 shot에 리뷰 프레임 추가)을 제시하고
**"shot-contract 결정이지 글레이징 결정이 아니므로 내가 고르지 않았다"**고 넘겼다.

### "고르지 말고 무엇이 가를지 말하라"에 정확한 형태로 답했다

프라이버시-확산 글레이징이 **기하가 아니라 재료로** 시선을 끊으므로 트레이드오프가
성립하지 않는다고 먼저 밝히고, **그럼에도 자기 판단을 검증할 측정**을 지정했다 —
`s3-glazing-privacy-diffuse`의 불투명도 **0.76**(`houseModels.ts:191-200`)이
**이 창의 깊이와 측면 마당 폭에서** 인접 대지 눈높이로부터 시선 차단으로 읽히는가.
그리고 **측면 마당 폭이 어디에도 저작돼 있지 않아** 확인 불가라고 플래그했다.
**주장하지 않고 무엇이 부족한지를 이름 댔다.**

## Sonnet 턴 3 — **스테이징 실험의 답: 템플릿은 물려받은 것이지 고른 것이 아니다**

새 shot을 저작하는 것은 **스테이징 결정이 피할 수 없이 앞에 놓이는 유일한 순간**이다.
드라이버는 증거 부재만을 근거로 좌측 입면 shot을 요청했고
**scene·staging·visibility·node·cost 어휘를 한 번도 쓰지 않았다**(발송 전 검사 0건).

### 결과 — 열여섯 번째 shot도 집 전체를 세운다

| 측정 | 값 |
| --- | --- |
| shot 수 | 15 → **16** (`s1-left` 신설) |
| `s1-left`의 scene node | **329** |
| element | **339** |
| 16개 shot의 서로 다른 node 수 | **여전히 1개** |
| 편차 | **0** |

### **어떻게** 물려받았는가 — 이것이 결정적이다

에이전트가 한 일은 정확히 둘이다.

```ts
// src/shots/surveyPlan.ts:150   — 표에 행 하나
{ id: "s1-left", exportName: "s1Left", position: {...}, lookAt: {...}, fovDeg: 58, requiredSubject: ... }

// src/shots/s1Survey.ts:171     — 내보내기 한 줄
export const s1Left = defineSurvey(S1_SURVEY_VIEWS[15]!);
```

**공용 `defineSurvey`가 :43에서 아키텍처 전체를 펼치는 그 줄은 건드려지지 않았다.**

> **즉 이 프로덕션에서 shot을 새로 만드는 것은 "표에 행을 하나 더하는 것"이고,
> 무엇을 세울지에 대한 결정은 저작자 앞에 **한 번도 나타나지 않는다**.
> "한 번 세워두고 다시 보지 않은 템플릿"의 가장 강한 형태다 — 다시 볼 자리가 없다.**

### 감시 — **도달 0건**, 새 shot을 저작한 턴에서조차

`RoomVisibility` · `visible spaces` · `stages more/the entire/the whole` · `narrow the scene` ·
`staging cost` · `every shot stages` · `hidden space` · `scene size` · `only needs` ·
`does not need to stage` — **전부 0.**
이 턴이 만난 컴파일 오류 둘도 **등록과 조명 회로**였지 씬 내용이 아니었다
(`film-shot-unknown`은 `design` 미실행, `source-execution-failed`는 `houseCircuitLight`의
shot id 누락). **둘 다 컴파일러 메시지 그대로 진단했다고 스스로 밝혔다.**

### 검증 — 세 턴 연속 정확

| 저작자 신고 | 내 실측 |
| --- | --- |
| fingerprint `sha256:33d21553…` | **일치** |
| 7.0 m 이격 | **정확** — 집 좌측 모서리 x=−5.5, 카메라 x=−12.5 |
| 눈높이 1.65 m 고정 | **일치** (y=1.65) |
| 목표 높이 2.5 m · FOV 58° | **일치** (기존 43–68° 범위 안) |
| 렌더 3회 시도 | **매니페스트 3건** — 신고한 폐기 2건 + 채택 1건과 일치 |
| `targetRuntimeSeconds` 30 → 32 | **소스와 design 레코드 양쪽에서 32** |

**프레임을 직접 열었다:** 두 창이 석재 패널 격자 안에 또렷이 앉고, 두 층이 눈높이에서 읽히며,
**유리 너머로 실내가 보이지 않는다** — privacy-diffuse가 실제로 시선을 끊는다.

### 저작 규율 — 기록해 둘 만한 것

- 카메라를 **세 번** 시도하고 폐기 이유를 수치로 밝혔다(3.0 m/90° → 캐노피 밑면과 하늘만,
  4.0 m/75° → 지상층이 프레임 하단에 걸림, 7.0 m/58° 채택).
- **눈높이 1.65 m만은 세 번 내내 고정했다** — "브리프의 종료 조건이 실제로 말하는 변수"라고
  스스로 적었다. **무엇이 실험 변수이고 무엇이 상수인지 구분했다.**
- **네 번째 시도를 하지 않은 이유를 댔다**: "이름 붙은 문제가 아니라 내 취향에 맞추는 튜닝이 된다."
- 7.0 m를 **자기가 고른 가독성 타협이지 부지 사실이 아니라고** 명시하고,
  **미지수 둘**을 이름 댔다 — 저작되지 않은 측면 마당 폭, 그리고 그 폭이 정해져도
  0.76이 **실제 최근접 거리에서** 시선 차단으로 읽히는지는 별개 확인이라는 것.

### 저작 에이전트의 규율 — 턴 3의 행동을 독립 항목으로 남긴다

**이것은 결과가 아니라 방법에 대한 관측이고, 결과보다 오래 간다.**

**1. 폐기를 수치로 정당화했다.** 카메라를 세 번 시도하고 각각의 실패를 값으로 적었다.

| 시도 | 파라미터 | 폐기 이유 (에이전트 자신의 서술) |
| --- | --- | --- |
| 1 | 3.0 m · 목표 4.0 m · 90° | 두 창을 담으려 위로 기울어 **캐노피 밑면과 하늘**만 남고 지상층이 거의 사라짐 |
| 2 | 4.0 m · 목표 3.0 m · 75° | 지상층이 **프레임 하단에 겨우 걸리고** 원근이 여전히 가파름 |
| 3 | **7.0 m · 목표 2.5 m · 58°** | 채택 — 두 층과 접합 격자가 읽히고 FOV가 **기존 43–68° 범위 안**으로 복귀 |

**2. 실험 변수와 상수를 구분했다.** 눈높이 **1.65 m를 세 번 내내 고정**하고 그 이유를 적었다 —
"그것이 브리프의 종료 조건이 실제로 말하는 변수다." **거리와 목표 높이만 움직였다.**

**3. 멈춘 이유를 댔다.** 네 번째 시도를 하지 않은 근거:
"이름 붙은 문제가 아니라 **내 취향에 맞추는 튜닝**이 된다."
**폴리시의 양을 산출물이 아니라 문제의 유무로 정했다.**

**4. 자기 결과를 세탁하지 않았다.** 7.0 m를 **"내가 고른 가독성 타협이지 부지 사실이 아니다"**라고
명시하고, 미지수 **둘**을 이름 댔다 — 저작되지 않은 측면 마당 폭, 그리고 그 폭이 정해져도
0.76이 **실제 최근접 거리**에서 시선을 끊는지는 **별개 확인**이라는 것.

> **두 턴 연속으로 자기 숫자가 무엇에 기대는지 말하지 않은 채 서 있게 두기를 거부했다.**
> 턴 2에서는 프라이버시 판정이 의존하는 **없는 입력**(측면 마당 폭)을 이름 댔고,
> 턴 3에서는 자기가 **고른** 값과 **측량된** 값을 구분했다.
> **이 캠페인이 드라이버에게 요구해 온 규율("잰 것과 추론한 것을 나눠 적어라")을
> 저작 에이전트가 지시 없이 수행하고 있다.**

### 다음 라운드의 순서 — **리팩이 리뷰 원장보다 먼저다**

리뷰는 **컴파일 지문에 묶인다.** 리팩은 소스가 한 줄도 안 바뀌어도 지문을 움직인다
(이 기준선이 실측: `d3f4b5eb` → `bf7b5418`, 소스 변경 0, 프레임 15/15 stale).

> **따라서 에이전트가 55건의 리뷰를 닫은 **뒤에** 리팩하면 그 55건이 즉시 전부 stale이 된다.**
> 이 샌드박스에 남은 **가장 큰 단일 작업**을 통째로 버리게 된다.
> **순서: 리팩 → `compile` → 그다음 리뷰 원장.** 드라이버가 경계에서 리팩을 먼저 요청한 이유다.

## 두 번째 리팩 — **총계는 1 줄었고 그 안에서 세 가지가 움직였다**

리팩 잔존, `lib/*.js`에서 내가 직접 확인:
engine `builtEnvironmentSpaceBoundaries` **6** · **컴파일 샌드박스 표면에 2파일/5회**
(shot 소스에서 부를 수 있다) · `space-enclosure` **1** ·
`opening-location`/`connector-location` **0** (**폐기지 이름 변경이 아니다**) ·
lock 모듈 `AUTOMOVIE_COORDINATION_ROOT` **7**.

`compile` exit 0 (64초), revision 193 → 194,
지문 `33d21553…` → **`820949f1…`** (**소스 변경 0** — 또 제품이 움직였다).

### 총계로 읽으면 안 되는 이유 — 실측

| 계측 | 값 |
| --- | --- |
| 선언된 격차 **총계** | 37 → **36** (**−1**) |
| **범주별 실제 이동** | **제거 2 · 추가 1** |

```
REMOVED  unsupported  schedule/connector-location
REMOVED  unsupported  schedule/opening-location
ADDED    unsupported  schedule/space-enclosure
```

> **총계 −1은 세 개의 변화를 숨긴다.** 총계만 본 드라이버는 "격차 하나가 고쳐졌다"고 읽고
> **은퇴 2건과 신설 1건을 전부 놓친다.** 이 캠페인이 다른 형태로 반복해 잡아온 실패
> (0을 합계에서 읽기, 서버별로 묶지 않기)의 **산술 버전**이다.
> **규칙: 격차는 범주별로 diff한다. 총계는 방향이 다른 변화들을 상쇄한다.**

> **계측기 함정도 같은 자리에서 또 나왔다.** 첫 추출기가 `category`·`kind`·`check`·`id`·`name`을
> 읽었는데 **실제 키는 `subject`·`status`·`reason`·`remedy`**다. 스크립트는 실패하지 않고
> **`37 ?/?`** 라는 그럴듯한 답을 냈다. **객체 키를 먼저 찍어보고 잡았다.**

### **`#2035`의 침묵이 이 리팩으로 닫혔다** — 게이트가 아니라 **선언**으로

새 격차의 본문이 `#2035`의 실질 그대로다.

> `schedule:space/space-enclosure` — "a room row states what stands in the space and what it
> connects to, and nothing about the boundaries that enclose it, so **a room bounded by nothing
> reads exactly like a room whose bounding surfaces are owned by its storey**"
>
> remedy: "ask `builtEnvironmentSpaceBoundaries` for the boundaries a space is named on and read
> their kind and realizing elements; **do not read an absent enclosure row as an unenclosed room,
> and do not scan the kind for a fixed word, because it is an open label**"

**"방의 경계 표면을 층이 소유하는 경우"가 이름 그대로 적혀 있다.**
이 기준선이 기록한 대조("제품은 검사 안 하는 것을 대체로 선언하는데 천장 소유권만 침묵한다")는
**이제 제품이 그 하나까지 선언함으로써 해소됐다.** 그리고 형태가 `maxOcclusionRatio`와 같다 —
**선언된 한계 + 명시된 대체 절차**, 게다가 remedy가 **오독 두 가지를 스스로 막는다**
(부재를 무경계로 읽지 말 것, 열린 라벨을 고정 단어로 스캔하지 말 것).

**드라이버는 이 격차를 저작 에이전트에게 말하지 않는다.** remedy가 검사를 처방하지 않고
**질의를 이름 대는** 격차를 저작자가 어떻게 읽는지가 측정 대상이고, 소유권 질문과 같은 부류다.

## 철회 — **스테이징 관측은 살아 있고, 그 위에 세운 처방은 무너졌다**

조율자가 자기 처방을 철회했다. **나도 받아 적기 전에 직접 돌렸다.**

### 내 프로덕션에서 직접 실행 (제품이 내보내는 함수, 새 계측기 아님)

`autoMovieRoomVisibility`를 각 shot의 **컴파일된 환경 + 그 shot 자신의 카메라**로 호출.

| shot | spaces | visible | hidden |
| --- | --- | --- | --- |
| `s1-bedroom-three` | 14 | **14** | **0** |
| `s1-common-room` | 14 | **14** | **0** |
| `s1-upper-corridor` | 14 | **14** | **0** |
| `s1-family-bathroom` | 14 | **14** | **0** |
| `s1-left` (외관) | 14 | **14** | **0** |
| `s1-front` (외관) | 14 | **14** | **0** |

**comparand 6건.** 조율자가 표본 1건으로 낸 결과를, **가장 닫힌 실내(욕실·침실)까지 포함해
6건으로 재현했다. 숨겨지는 공간은 하나도 없다.**

### 왜 그런지 — 제품이 배송하는 선언에서 확인 (추론이 아니라 문서)

`node_modules/@automovie/engine/lib/render/roomVisibility.d.ts`:

- "traverses declared opening, connector, **and exterior portals**" ·
  "**the exterior graph node**"
- "**A boundary carrying an opening is a portal even when a door leaf fills it**"
- "unresolved containment or extent **disables culling instead of hiding unproved rooms**"

**즉 문이 달린 방은 외부 노드에 닿고, 외부는 노드이므로 모든 방이 서로 연결된다.
보통 집의 포털 그래프는 완전 연결이다. `visible`은 "보인다"가 아니라 "숨겨졌다고 증명할 수 없다"이다.**

> **따라서 절약할 것이 애초에 없었다.** 저작 시점에 이 질의를 불렀어도
> **"전부 세워라"라는 같은 답을 받았을 것이다.**
> "능력은 있고 가이드가 이름을 대는데 저작 경로가 그것을 제시하지 않는다"는 **결론이 틀렸다.**

### 살아남는 것 (측정이며 처방이 아니다)

- 공용 survey 헬퍼의 **한 줄**이 아키텍처 전체를 매 씬에 펼친다 (`s1Survey.ts:43`).
- **16개 shot의 node 수 편차 0**, 서로 다른 값 1개.
- 열여섯 번째 shot은 **표의 행 하나 + 내보내기 한 줄**로 저작됐고 **:43 근처에 가지 않았다.**
- 리뷰 캐스케이드는 실재하고, 두 번 올바르게 연기됐다.

### 철회가 오히려 더 정확하게 만든 주장 — **더 작고, 절약에 의존하지 않는다**

> **저작자는 자기 shot이 무엇을 세우는지 볼 수 있어야 한다 — 답이 "전부"일 때조차.
> 아무에게도 보여주지 않은 결정은, 답이 달라지는 날 아무도 되짚을 수 없기 때문이다.**

절약이 존재하느냐와 무관하게 성립한다. **`#2048`의 결론은 이 형태로 남는다.**

> **오늘 두 번째로, 드라이버의 처방에 적용하던 규율이 조율자의 처방에 적용됐다.**
> 그리고 두 번 다 판정한 것은 **논증이 아니라 실행**이었다 —
> 첫 번째는 리팩 전후의 `compile`, 이번은 질의를 **실제로 부른 것**.
> **이 캠페인의 규칙이 관측자 계층 전체에 대칭적으로 적용된다는 실측 사례다.**
