# Repaint handoff

## Stable hint-motion inputs {#stable-hint-motion-inputs}

The repaint lane receives deterministic beauty and guide-pass inputs, exact timing, and provenance tied to the compiled shot and capture receipt. Repaint may change appearance while preserving structure, and its output never replaces the structural evidence that grounded it.
