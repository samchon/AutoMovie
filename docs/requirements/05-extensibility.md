# Extensibility

## Additive typed growth {#additive-typed-growth}

New subject, motion, world, staging, sound, analysis, and render capabilities extend public discriminated contracts and deterministic consumers without reconstructing hidden source data. Compatibility is preserved through explicit optional records and validated defaults, not silent inference.
