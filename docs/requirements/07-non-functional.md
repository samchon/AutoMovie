# Non-functional contract

## Portable, licensed, and proved {#portable-licensed-proved}

The repository supports its declared Node and pnpm toolchain on Windows and POSIX paths, ships only license-compatible production dependencies, and keeps every changed source file at 100 percent statements, branches, functions, and lines. Package builds and the root build remain separate compile gates.
