# Determinism

## Same inputs, same artifact {#same-inputs-same-artifact}

The same declared inputs, package versions, asset bytes, seed, frame clock, and operation order produce the same canonical records and rendered schedule. Wall time, ambient randomness, filesystem enumeration order, and platform-specific path spelling cannot decide product output.
