# Ensembles and scale

## Explicit bounded multiplicity {#explicit-bounded-multiplicity}

Many subjects remain authored as compact identities, layouts, representative models, and budgets rather than expanded copies. Every expensive per-subject or per-frame operation declares and reports its bounded population so scale cannot silently change the algorithm.
