# External inputs

## Proven bounded ingestion {#proven-bounded-ingestion}

Every external model, motion, image, audio, and design reference enters as declared resident bytes with digest, provenance, consumer identity, and a supported bounded decoder. Unsupported formats, missing ownership, incompatible rigs, and digest changes are explicit refusals, and the repository does not ship a content catalogue to avoid them.
