{
  "private": true,
  "name": "@motica/test",
  "version": "0.1.0",
  "description": "motica test program — exhaustive deterministic engine validation",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "ts-node --transpile-only -P tsconfig.json src/index.ts",
    "coverage": "cd .. && c8 --src packages/engine/src --include 'packages/engine/src/**' --extension .ts --temp-directory /tmp/motica-c8 --reports-dir /tmp/motica-c8-report --reporter=text-summary --check-coverage --branches 100 --functions 100 --lines 100 --statements 100 ts-node --transpile-only -P test/tsconfig.json test/src/index.ts"
  },
  "author": "Samchon",
  "license": "MIT",
  "dependencies": {
    "@motica/engine": "workspace:^",
    "@motica/interface": "workspace:^",
    "@nestia/e2e": "catalog:samchon"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "c8": "^10.1.3",
    "chalk": "4.1.2",
    "ts-node": "catalog:typescript",
    "typescript": "catalog:typescript"
  }
}
