/**
 * One comparable leaf value in an ordered plan field.
 *
 * Named so the comparison's domain is stated in the type rather than only in
 * prose: a speaker label is a string, a downmix coefficient is a number, and
 * nothing else is compared.
 */
export type AutoMovieOrderedScalar = number | string;
