---
name: contract
description: Locates the shared and production-owned contracts that this generated project answers. Use before interpreting, selecting, revising, or citing a discovery, upstream, principle, obligation, language, or production-local contract item.
---

# Contract

This is an index, never an evidence target. Read the generated `AGENTS.md` inventory, then open [the project-local contract root](../../../docs/README.md) and every selected document that governs the unit. Cite shared items by their `discovery/...`, `upstream/...`, `principles/...`, or `obligations/...` evidence root, language items by `language/...`, and production-owned additions through their declared local-claim root. Change reusable or language contract bytes only through an explicit upstream migration.
