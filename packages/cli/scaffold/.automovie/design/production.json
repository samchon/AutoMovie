{
  "id": "{{name}}",
  "title": "{{name}}",
  "logline": "A primitive sentinel raises and answers a signal before a deterministic instanced army.",
  "targetRuntimeSeconds": 11.5,
  "frameFormat": {
    "width": 1280,
    "height": 720,
    "fps": 24,
    "colorSpace": "srgb"
  },
  "artDirection": {
    "style": "primitive-3d",
    "palette": ["#182235", "#d7b56d", "#b13f36"],
    "silhouettePriority": "The sentinel and raised arm remain distinct from the ground and background.",
    "scaleGrammar": "A named sentinel and promoted formation heroes remain readable against simultaneous near and far anonymous ranks."
  },
  "deliverables": [
    {
      "id": "starter-preview",
      "kind": "preview",
      "required": true
    },
    {
      "id": "starter-feature",
      "kind": "feature",
      "required": true
    },
    {
      "id": "starter-pose-guide",
      "kind": "guide-pass",
      "required": true
    },
    {
      "id": "starter-captions",
      "kind": "captions",
      "required": true
    },
    {
      "id": "starter-audio",
      "kind": "audio-mix",
      "required": true
    }
  ]
}
