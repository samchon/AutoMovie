{
  "id": "{{name}}",
  "title": "{{name}}",
  "logline": "A primitive sentinel raises and answers a signal across a deterministic two-shot film.",
  "targetRuntimeSeconds": 12,
  "frameFormat": {
    "width": 1280,
    "height": 720,
    "fps": 24,
    "colorSpace": "srgb"
  },
  "artDirection": {
    "style": "primitive-3d",
    "palette": ["#182235", "#d7b56d", "#b13f36"],
    "silhouettePriority": "The sentinel and raised arm remain distinct from the ground and background.",
    "scaleGrammar": "One hero primitive across two shots establishes compile, preview, guide-pass, aggregate render, and film-review wiring."
  },
  "deliverables": [
    {
      "id": "starter-preview",
      "kind": "preview",
      "required": true
    }
  ]
}
