# Treatment — {{name}}

## SEQ-SIGNAL — The call

A lone sentinel raises a signal while an ordered army remains visibly under
the same command.

## SEQ-ANSWER — The reply

The answering gesture proves the signal changed the field rather than ending
as an isolated pose.
