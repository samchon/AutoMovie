<!-- @evidence docs/{{name}}/01-logline.md Carries the raising of the signal the logline promises. -->

# SEQ-SIGNAL — The call

A lone sentinel raises a signal while an ordered army remains visibly under the
same command.

The sequence exists to make the gesture legible as a command rather than as an
isolated pose, so the army has to read as ordered while it happens.
