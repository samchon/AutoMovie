<!-- @evidence docs/{{name}}/01-logline.md Carries the raising of the hand the logline promises. -->

# SEQ-CUE — The call

A soloist raises a hand while the chorus behind stays visibly in its rows.

The sequence exists to make the gesture legible as a cue rather than as an
isolated pose, so the rows have to read as rows while it happens.
