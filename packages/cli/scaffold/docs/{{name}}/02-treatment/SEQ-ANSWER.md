<!-- @evidence docs/{{name}}/01-logline.md Carries the army's answer the logline promises. -->

# SEQ-ANSWER — The reply

The answering gesture proves the signal changed the field rather than ending as
an isolated pose.

The sequence exists so the army is the visible consequence of the exchange
instead of anonymous background.
