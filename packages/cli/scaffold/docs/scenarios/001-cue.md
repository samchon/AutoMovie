<!--
@evidence principles/common.md#change-upstream-first Refines the storyline rather than restating it; a disagreement with the intent is repaired in the storyline first.
@evidence principles/scenarios.md#one-parent Refines exactly the cue sequence and nothing else.
@evidence principles/scenarios.md#stageability Everything it asks for — the figure, the group, the ground, the named point the figure stands on — is a settings fact, and the only thing it asks to move is an arm.
@evidence principles/scenarios.md#entry-and-exit Begins with the hand down and the group in rows, and ends with the hand up and the group unmoved.
@evidence principles/scenarios.md#settings-rechecked Cites the ground and the figure directly rather than inheriting the storyline's reading of them.
@evidence storylines/001-cue.md Refines the cue sequence into the one staged action it turns on.
@evidence settings/010-soloist.md Stands the figure ahead of the rows, which is the separation that document fixes.
@evidence settings/040-plaza.md Stands the figure on `plaza-center`, the named point that document provides for exactly this.
@evidence principles/common.md#determinism Fixes the staging as a placement and one arm, so nothing below has to decide where the figure ends up.
@evidence principles/common.md#stated-basis Names `plaza-center` as the point the figure stands on rather than describing a distance nobody measured.
@evidence settings/050-art-direction.md Stages the figure at the review distance that document fixes.
-->

# Cue — the hand goes up

**Entry state.** The chorus stands in its rows on the plaza. The soloist stands
on `plaza-center`, clear of the front row, with the hand down.

**Action.** The soloist raises one hand and holds it there. The chorus holds its
rows behind the gesture, and nothing else on the ground moves.

**Exit state.** The soloist stands where it began, with the hand up. The chorus
has been addressed and has not moved.

The separation the cue needs is a placement rather than an approach: the soloist
is already clear of the rows when the scene opens, so the one thing that changes
on screen is the arm. Staging it as a walk would put a second moving subject in
a frame whose whole job is to make one gesture legible.

The hold after the hand reaches its height is what makes this a cue rather than
a pose; the script fixes how long it is.
