/** Compiler-owned runtime identity for one project model recipe. */
export const productionRuntimeModelId = (recipe) => `automovie:model:${recipe}`;
/** Compiler-owned skeleton identity for one rigged project model recipe. */
export const productionRuntimeSkeletonId = (recipe) => `automovie:skeleton:${recipe}`;
//# sourceMappingURL=productionIdentity.js.map